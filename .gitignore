# Per-machine Claude Code state — never sync via git
.claude/settings.local.json
.claude/worktrees/

# Node
node_modules/

# Environment files
.env
.env.local
.env*.local

# Vercel
.vercel/
.vercel

# Claude Code project memory — keep out of this public repo
.claude-memory
.env*

# Playwright test artifacts
.playwright-mcp/

# remember plugin state
.remember/
screenshots/

# Session-local review artifacts — screenshots + notes stay off the public repo
.claude/
390/

# walker scratch dirs (audit runs) — never commit; contain crew tokens
390/
768/
1440/
offline/
baseline-*.png
