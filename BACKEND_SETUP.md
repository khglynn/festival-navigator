# Backend Setup — obsolete

This file described an early **Supabase** plan that was never built. The app
uses **Vercel Blob** for group sync (see `api/selections.js`).

For real setup, see **[VERCEL_SETUP.md](VERCEL_SETUP.md)** and the **One-time
setup** section of the [README](README.md).
