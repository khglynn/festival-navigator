# v3.1 merged backlog — the fix session's working index

**Synthesized 2026-07-11 from three independent sources.** This is an INDEX —
one line per finding, ranked within class. Full evidence lives in the source
docs; read them per-class as you fix:

- **WF** = `claude-plans/2026-07-11-v31-backlog-workflow.md` (54-agent audit: walked live at
  390/768/1440, adversarially verified — richest evidence, screenshot paths)
- **CX** = `claude-plans/2026-07-11-codex-v31-ux-review.md` (Codex gpt-5.6-sol blind code pass,
  51 findings — richest root causes)
- **KEV** = Kevin's own pass (inventory in
  `claude-plans/2026-07-11-v31-audit-first-plan.md`)

Severities re-judged at merge (sources sometimes disagreed; the call and its
reason are the merge's job). **Multi-source = independently confirmed — trust
those first.** Design decisions for the D-class fixes live in
`claude-plans/2026-07-11-v31-design-direction.md`.

Counts: **1 P0 · 32 P1 · 40 P2 = 73 items** across 9 fix classes.
(Corrected at triple-check — the first commit's header and message said "57";
the itemized list below was always the truth. Count it, don't trust headers:
`grep -oE '\*\*P[0-2] [A-Z]+-[0-9]+\*\*' <this file> | sort | uniq -c`.)

## Calibration verdict (why this list is trustworthy)

The machinery independently rediscovered 15/16 of Kevin's findings. The one
miss — SPOT-1's live OAuth redirect — failed for a structural reason (audit rig
had no Client ID, so no walker could reach the redirect). Lesson banked: the
Stage-4 re-run MUST seed a Client ID and walk F13 end-to-end. Nothing else in
Kevin's list was missed, and the machinery found ~45 problems beyond it.

---

## Class 1 — CORE (broken behavior users hit today) — fix first

- **P1 CORE-1** Set-times tap makes the card vanish (refreshCard drops grid
  placement; non-topmost cards land in the hour-rail column off-canvas).
  `wall.js:132-136` vs `229-244`. [WF-E1 · KEV-H1] → fix + regression test.
- **P1 CORE-2** Hour rail invisible at every viewport (scroll-snap pins
  scrollLeft past it; labels not sticky) — the set-times view shows no time
  axis. `v3.css:174-181`. [WF-F1]
- **P1 CORE-3** Scheduled cards never render their start time (renderCard
  drops opts.time). `wall.js:53,232`. [WF-F2b · CX]
- **P1 CORE-4** Search in a scheduled fest loses stage/time context and can't
  answer "where and when." `wall.js:279-309`. [CX]
- **P1 CORE-5** Sort control silently no-ops in scheduled view. `wall.js:279`.
  [CX]
- **P1 CORE-6** Note-only remote syncs never repaint (applyRemoteDoc ignores
  notes in its diff) — live crew notes don't appear. `state.js:173-186`. [CX]
- **P1 CORE-7** "Download day image" produces a 0-byte PNG silently (captures
  a display:none wall; no day scoping; errors swallowed). `tools.js:98-106`.
  [WF-C3 · CX]
- **P1 CORE-8** Bulk-paste integrity cluster: unknown artists counted as
  applied; unknown level labels become real level-1 picks; "applied" while
  migrationPending discards everything. `tools.js:60-90`, `parse.js:18-22`,
  `app.js:255`. [CX ×3]
- **P1 CORE-9** Add-a-festival can silently REPLACE canonical catalog data on
  id collision (custom upsert wins over checked-in fest). `festivals.js:34-37`,
  `api/festival-add.js:92`. [CX] — data-integrity class, guard at save time.
- **P1 CORE-10** Empty-lineup fests lose the festival-notes composer entirely
  (early return) — notes are most valuable pre-lineup. `wall.js:292-317`. [CX]
- **P1 CORE-11** Add-a-festival Save can wedge disabled forever after a network
  error (no try/finally). `settings.js:214-220`. [CX]
- **P1 CORE-12** Offline switch to an uncached fest strands the app (persists
  active id before load; boot retries the broken fest). `app.js:243,355`. [CX]
- **P2 CORE-13** Settings pick-counts count cleared picks (raw keys vs
  picksFor) and can include bulk-paste ghosts. `settings.js:104-128`.
  [WF-E3 · CX]
- **P2 CORE-14** Spotify Client-ID save dead-ends the open drill (no re-render;
  "connect below" never appears). `settings.js:392-416`. [WF-E2 · CX]
- **P2 CORE-15** Long-press orphaned by poll repaint (no pointercancel; timer
  fires on detached node); multi-day sibling cards go stale on tap and invite
  double-cycling. `wall.js:93-109,132-136`. [WF-E4]
- **P2 CORE-16** Open note sheets never receive live crew updates. `notes.js`.
  [CX]
- **P2 CORE-17** Migration-blocked toast shows a fake Undo button. `app.js:47`,
  `wall.js:332-335`. [CX]
- **P2 CORE-18** Legacy v3 crew offline = all picking blocked with no
  explanation (migration gate). Surface a persistent banner + retry; staging
  path optional. `app.js:348-351,46`. [CX]

## Class 2 — FLOWS (joins, navigation, lost states)

- **P0 FLOW-1** Invite opened on a new device loses festival context — joiner
  lands on defaultFestivalId() (Lost Lands, empty wall) instead of the crew's
  fest. The app's distribution loop breaks at first impression. Fix DECIDED at
  triple-check (don't re-litigate): hybrid. (a) New share links carry
  `&f=<festId>` in the hash — read at join BEFORE app.js:363's replaceState
  wipes it; zero-write, instant. (b) ONE additive optional validated meta
  field (`meta.inviteFestId`) written through the normal merge path — fixes
  already-distributed links too, after a single additive merge per real crew
  (additive-through-app-API is already permitted). This ONE field is
  explicitly carved out of the crew-doc shape freeze (see grounding doc);
  everything else stays frozen. Old token-only links with neither signal keep
  today's fallback.
  `crew.js:45`, `state.js:57-59`, `app.js:220`. [CX-P0 · WF-G3.4 — the only
  double-flagged load-bearing finding; treat as the run's #1]
- **P1 FLOW-2** Browser back/forward don't model pages/sheets — back ejects to
  blank/stale (all overlays historyless). Minimal route model per design doc.
  `app.js:236-262,429`. [WF-C4 · CX · KEV-G1]
- **P1 FLOW-3** Bad/expired/offline `#g=` link silently falls to landing; no
  "link didn't work" state (F16.2 never built); dead crews stay as remembered
  rows (forgetCrew never called). `app.js:369-391`, `crew.js:49-54`.
  [WF-C2 · CX]
- **P1 FLOW-4** Uncaught boot/enterApp exception = permanently blank page (no
  error boundary, all screens display:none). `app.js:337-385`. [WF-C1]
- **P1 FLOW-5** Join accepts names the server rejects (apostrophes etc.) —
  member picks locally, nothing ever syncs, only a gray dot hints. Validate
  client-side with the server rule + block entry on failed first write.
  `app.js:319`, `crew-shared.mjs:29`. [CX]
- **P1 FLOW-6** No crew switcher/leave/forget once a crew is active; landing
  unreachable; no crew rename anywhere in v3 (regression — old app.js had it,
  deleted wholesale in bd659ca). `app.js:264,375`, settings. [CX · WF-G2]
- **P1 FLOW-7** Create never leads to the invite/share step — the collaborative
  loop is buried in Settings. Post-create share moment per design doc. [CX]
- **P1 FLOW-8** Person chips silently switch device identity on tap. Move
  identity change behind an explicit control + confirmation. `app.js:95-105`.
  [CX]
- **P2 FLOW-9** Two-step create + past-fests section at create (one flat screen
  today; hard archived filter). `app.js:170-204,176`. [WF-G3 · CX · KEV-C1/C2]
- **P2 FLOW-10** Join screen never names the festival (headline is crew name
  only). `app.js:296-298`. [WF-G3.3]
- **P2 FLOW-11** Settings lacks the spec's two doors: no crew section (rename/
  danger zone), no self rename/color change; archived fests in fine-print.
  [WF-G2 · KEV-H3]
- **P2 FLOW-12** Share-invite failures silently read as cancellation; no
  fallback visible URL. `settings.js:97-102`. [CX]
- **P2 FLOW-13** Enter key ignored on create/join/research forms. [CX]

## Class 3 — DESKTOP (the "stretched mobile" class — design doc governs)

- **P1 DT-1** No desktop day navigation (dock hidden ≥720, no replacement) →
  day rail per design doc. `v3.css:136`. [WF-B1 · CX]
- **P1 DT-2** Sheets are full-width bottom strips at 1440 → centered dialog
  ≥720 per design doc. `v3.css:104-110`. [WF-B2 · CX · KEV-A2]
- **P2 DT-3** Type never scales; wall grid caps at 4 cols in a 960px shell;
  dead margins at 1440 → clamp token scale + auto-fill grid + shell-max token.
  [WF-B3 · KEV-A1]
- **P2 DT-4** Entry screens top-pinned with dead lower half → margin-block:auto
  centering (404.html already has the pattern). [WF-B6 · KEV-A1]
- **P2 DT-5** Set-times trapped in the 960px shell — 7-stage EF can't fit →
  full-bleed timetable wall per design doc (the run's one aesthetic risk).
  [CX · KEV-A3]
- **P2 DT-6** No hover/visible note affordance on cards for pointer users
  (chicken-and-egg: chip only renders when noteCount>0) → hover ✎ per design
  doc. `wall.js:93-109`, `aura.js:86`. [WF-B4]
- **P2 DT-7** Sort control reads as a static label (appearance:none, caret CSS
  can never render on a <select>) → styled listbox per design doc, keyboard
  parity required. [WF-G5 · CX · KEV-B1]

## Class 4 — A11Y (absent layer; fold into every touched component + sweep)

- **P1 AX-1** Cards keyboard-unreachable, zero ARIA — pick + notes pointer-only.
  Real button semantics + Enter/Space + level-in-accessible-name. `wall.js`.
  [WF-A1 · CX]
- **P1 AX-2** outline:none on all 7 inputs, zero :focus-visible anywhere.
  [WF-A2]
- **P1 AX-3** --text-tertiary #5D5578 = 2.84:1 on page bg — fails AA on ~30+
  real-copy sites (and this app is read in daylight). Retune the token (keep
  the hue, raise L until ≥4.5:1 on --page/--card) + verify all three surfaces.
  `v3-tokens.css:24`. [WF-A3]
- **P2 AX-4** Sheets lack dialog semantics/focus management (Esc + backdrop DO
  work). [WF-A4]
- **P2 AX-5** No labels on identity/composer/client-id inputs (search is the
  one correct example — copy it). [WF-A5]
- **P2 AX-6** Touch targets under 44px (dock avatar 26px, gear 27px, day tabs);
  390px day-tab labels clip to "IU"/"SI". [WF-A6]
- **P2 AX-7** Settings rows are clickable divs; toggle switches unnamed.
  [CX]

## Class 5 — NOTES (the IA Kevin flagged + what the engines added)

- **P1 NT-1** All-notes sheet is read-only (no composer any scope, empty state
  redirects to gestures) → notes home per design doc. `notes.js:137-173`.
  [WF-G1a · KEV-D1]
- **P1 NT-2** Day headers have no notes affordance/count → day-header chip.
  `wall.js:151-165`. [WF-G1b · KEV-D2]
- **P1 NT-3** Notes can't be edited or deleted after posting — the tombstone
  model exists, no UI writes it. Author-only edit/delete. `notes.js:40-52`.
  [CX]
- **P2 NT-4** Pins only work in under-day sections (artist sheet + all-notes
  never pass onPinToggle). `notes.js`. [WF-G1c]
- **P2 NT-5** Sheet grabber advertises a swipe that doesn't exist; no visible
  close button. [CX]

## Class 6 — SET-TIMES DATA SURFACE (beyond CORE geometry)

- **P2 ST-1** Multi-day artist becomes a combined "SATURDAY & SUNDAY" section +
  duplicate indistinguishable SAT dock tabs → split into real days at render
  (groupByDay against known day names) + validator warning. `wall.js:141-149`,
  `app.js:143`. [WF-F3 · CX · KEV-E1]
- **P2 ST-2** Stage-less programming is a below-grid list → far-right
  "everything else" column per design doc; schedule-place anything with
  stage+time. `wall.js:248-269`. [WF-F2 · KEV-H2]
- **P2 ST-3** ACL weekends (W1/W2/both — validated, imported, documented as "a
  weekend filter") drive nothing; wrong-weekend musts possible on a LIVE fest →
  weekend selector/badges. [WF-H2 · CX]
- **P2 ST-4** Day headers omit real dates (dayMeta.date never rendered).
  `wall.js:192-193`. [CX]
- **P2 ST-5** Archived fests look/behave like live planning (no archive banner;
  picks editable unlabeled). [CX]
- **P2 ST-6** Lolla 2025 has empty year/dates everywhere; add validator
  completeness rule for archived fests. [CX · WF]

## Class 7 — SPOTIFY (functional break + flow redesign)

- **P1 SPOT-1** OAuth redirect_uri rejected — kevinhg.com domains not
  registered in the Spotify app. Code side: canonicalize connect to ONE domain
  before redirect. ⚠️ KEVIN ACTION: register `<canonical>/spotify-callback` in
  the Spotify developer dashboard. `spotify.js:13`. [KEV-I1 — machinery's one
  structural miss; ground truth is Kevin's screenshot of Spotify's error]
- **P1 SPOT-2** Drill is a floating button in a void; Client-ID step reads as
  user config with zero guidance (the "setup guide in the UI" claimed in code
  comments does not exist) → five-state card per design doc. [WF-B5 · CX ·
  KEV-I2]
- **P1 SPOT-3** Wrong Client ID is uncorrectable (input only renders when id
  empty; Disconnect doesn't clear it) → crew Spotify config view/edit/clear.
  `settings.js:392-477`. [CX]
- **P1 SPOT-4** SW serves stale Spotify API responses cache-first (api.spotify
  .com dodges the /api/ guard) — every re-scan is one scan stale, silently.
  Fix the SW guard to same-origin-/api/ OR network-only for cross-origin.
  `service-worker.js:71-90`. [WF-D1]
- **P1 SPOT-5** Library scan re-runs from scratch per festival; cached
  libraryMap never reused on fest switch. `settings.js:426-431`. [CX]
- **P2 SPOT-6** Access-request flow (api/access.js) fully built server-side,
  unreachable in UI — wire it or park it consciously (Kevin queued it as a
  fast-follow pre-v3.1; now two engines flagged it). [CX · KEV fast-follow]
  [retired: PARKED consciously at fix-phase (2026-07-12) — Spotify dev-mode
  allowlisting is a Kevin-owned dashboard workflow; the five-state drill's
  "ask your crew lead" copy covers the UX gap until the fast-follow. Server
  code stays.]
- **P2 SPOT-7** Playlist writes 2 tracks/artist while UI promises one.
  `settings.js:452-465`, `spotify.js:171`. [CX]

## Class 8 — PWA/SYNC HONESTY

- **P2 PS-1** SW install swallows per-asset failures then activates as
  offline-ready — incomplete shells ship. Atomic core set. `service-worker.js:
  44-51`. [CX]
- **P2 PS-2** Stale-SW can serve an old shell on returning devices (mechanism
  confirmed, live repro missing) — network-first for navigations; scripted
  stale-SW test. [WF-D2]
- **P2 PS-3** No offline event handler — green dot lies up to 5 min. [CX]
- **P2 PS-4** Hung fetch jams sync forever (no timeout/AbortController;
  isSyncing never clears). `sync.js:36-123`. [WF-C5]
- **P2 PS-5** Settings "synced" label computed from hasPending only —
  contradicts the dot, lies offline. One observable sync state, rendered
  everywhere. [CX]
- **P2 PS-6** localStorage writes unguarded on the hot path (reads are
  wrapped; festivals.js already shows the pattern). [WF-C6]
- **P2 PS-7** Manifest locks portrait — kills landscape schedule reading.
  `manifest.json:9`. [CX]

## Class 9 — CONTENT & FIRST-RUN

- **P2 CT-1** No onboarding: the pick mechanic, auras, who-corners and must
  semantics are un-inferable; the one legend is 3 taps deep in Settings.
  Lightweight first-wall coach-mark + point to How-it-works. [WF-H1]
- **P2 CT-2** Gemini preview shows 12 of N artists, a source COUNT (often 0),
  no full-lineup review before "Looks right — save it"; discard leaves stale
  text; error copy promises a manual path that doesn't exist; the subview
  stacks on top of Settings (only drill that doesn't hide main). [CX ×3 ·
  WF-G4]
- **P2 CT-3** Copy pass on all empty/error states per design doc writing
  rules (invitation to act, active voice, action keeps its name). [design doc]

---

## Fix sequencing (judgment, not just severity)

1. **CORE + FLOW-1/2/3/4** — broken things and the distribution loop. FLOW-1
   first: it's double-flagged, load-bearing, and small.
2. **DESKTOP + A11Y foundations** (tokens: type scale + contrast + focus) —
   these change every screen; do before per-surface polish so later classes
   land on the new system.
3. **NOTES + SET-TIMES** — the experience classes.
4. **SPOTIFY** — code side + queue Kevin's dashboard action.
5. **PWA/SYNC + CONTENT** — honesty + first-run.
6. Stage-4 audit re-run (with Client-ID seeded rig + offline pass — the two
   named coverage gaps) + Codex diff gate + taste pass.

Escape hatch: if a finding proves wrong or already-fixed when you reach it,
mark it `[retired: reason]` in this file rather than silently skipping — the
Stage-4 re-run reads this list as its expectation.
