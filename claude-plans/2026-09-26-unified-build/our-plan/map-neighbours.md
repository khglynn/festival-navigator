# Map: neighbours of Our plan (parallel builds + designs that touch it)

Status: complete (2026-09-26 ~05:05 PT). Head mapped: live/plan @ e1eb206, re-checked at 6d80134 (docs-only merge; code lines unchanged). Neighbours: origin/live/list c5e436b + uncommitted WIP, origin/live/tap e700c10, origin/live/v93 7abb52b (v96), origin/main b03f2ba.

## 1. The builds beside ours (fetched 2026-09-26 04:03 PT)

### 1a. Where everything stands, in one table

| Branch | Tip | What it is | Code changed vs our base aba41c3 |
|---|---|---|---|
| origin/main | 0fd8fb7 | v95 live (CACHE_VERSION `festival-nav-v95`, service-worker.js:5) + round-five design docs | — (v93 is NOT on main: 41 commits of live/v93 are only on our branch and its siblings) |
| origin/live/v93 | 3599950 | v93, "ships as v96" (arc NOW.md) | ONE commit beyond what we carry: 3599950 `wallUrl(token)` in app.js (lookAround/joinFromLayer ~app.js:700-718, enterApp's `history.replaceState` ~app.js:3204). Not in HEAD. Harmless to us, but we will inherit it on the next merge of main/v96. |
| origin/live/list | 49d3e04 | Phase 1 List + menu bar | Committed: only `.gitignore` (+`list-shots/`) and `LIST-BUILD.md`. **Uncommitted WIP in `.claude/worktrees/list`** (read, not touched): js/crew.js, js/v3/app.js (+76/-23), js/v3/filters.js (+49), js/v3/wall.js (+8), two new tests. |
| origin/live/tap | f84b671 | the tap-opens-the-notes-shelf change | Only `TAP-BRIEF.md`. Worktree `.claude/worktrees/shelves` clean. |
| origin/live/import | a17bf77 | import picks from a schedule export (part of v96 per arc NOW) | vs main: new js/fold.mjs, js/v3/import.js, js/v3/import-match.js, api/import-schedule.js; app.js hunks at ~64 (import), ~1925 (+70 lines after `seedShowOnce`), ~2320 (openSettings), ~3547 (init); v3.css +81 appended at the END of the file (after line 1482 on main); wall.js moves `searchFold` out to fold.mjs (wall.js ~20 import, ~874-890); **service-worker.js APP_CORE +3 lines** (`/js/fold.mjs` after name-rules.mjs; `/js/v3/import.js`, `/js/v3/import-match.js` after tools.js). |

All three unified-build branches (list, tap, plan) sit on aba41c3 (= v93 @ 9548b48 + main's round-five docs, merged 19f1e2d). None has moved code yet except list's uncommitted WIP.

### 1b. What live/list's WIP does (read from its working tree, 04:03 PT)

1. `ctx.view` = `'board' | 'list'` (new field in the ctx literal after `folded`, ~app.js:110), set in `refreshCtx` (~app.js:226) from `filters.js loadView(fid)` gated by new `wall.js listOffered(fest)` (a fest with `days` — inserted right after `roomsIn`, ~wall.js:1442).
2. Renames `seedShowOnce` → `seedViewOnce(fid, {show, view})` (~app.js:2051); `shareView`/`inviteLink`/`inviteViewLine` carry `&view=list`; `crew.viewFromHash()`; `pendingViewHint` beside `pendingShowHint` (boot ~app.js:3427-3443); enterApp's toast "Opened on X, as a list." / "Opened as a list.".
3. LIST-BUILD.md's plan (not yet coded): list renderer inside `wall.js renderComposed` (wall.js:2233) choosing the by-time body for every room (`timeGroups`, wall.js:1666); cards as `.card.row`; **the past fold as a pass at the end of the render against a FROZEN clock `ctx.pastAt`, removing over cards from the DOM**, recomputed at boot, on resume (visibilitychange) and on the festival-day turn; a whole-days "EARLIER · THU · FRI" line at the top of the wall in BOTH views; menu bar `dot · FEST '26 · ☰` in dock and rail; the menu's Board · List row.
4. Its own two product calls (LIST-BUILD.md "Product calls"): AFTERS on hours in the List; Board keeps rooms whole, only the whole-days line folds there.

### 1c. Where Our plan's edits collide (by file)

**js/v3/app.js** — the hot file; all three builds edit it.
1. `repaintWall` (app.js:1628-1651). Our plan adds the model build here (PLAN §2.6.4). List will touch the same function or its callees (the fold pass, the view switch's place-by-time). Keep our addition to ONE line near the end (after `positionNowMarks`, app.js:1644) calling a plan-shelf function, so the hunk does not overlap list's.
2. `tickClock` (app.js:824-832). Ours adds `planAt/peekOf` and the one-NOW decision; list will add "recompute the past fold when the festival day turns" and probably a resume hook. Both append to the same 8-line function — **likely textual conflict**. Suggest: we add one line `paintPlan(date)` directly BEFORE `paintNowTabs(date)` (the one-NOW rule needs the peek's state first), and ask list to append its line after `paintNowTabs`.
3. `startClock`'s visibilitychange listener (app.js:822) — list's "recompute on resume" (LIST-BRIEF item 2) may hang here; ours needs nothing new (tickClock already runs on visible).
4. `paintNowTabs` (app.js:856-862) — ONLY ours changes its logic (the `&& !peekShowsNow` gate). v93 owns the placement machinery below it (showNowTab etc., app.js:863-954); do not touch those. List's brief item 7 ("NOW in the list … the NOW jump work") may make `nowLanding` see list cards, but that is wall.js, not the door. Low risk.
5. `seenBand` (app.js:959-971) reads `#dock`'s top; the peek must be counted (PLAN §2.5 `--foot-h`). Only ours edits it. Note: list's past fold and view switch hold the place by time — they read the top edge, not the bottom, so no semantic clash.
6. `measureStickyChrome` (app.js:1333-1345) is where `--foot-h` would be written; nobody else edits it today.
7. `openShowMenu`/`closeShowMenu` (app.js:1459-1510) and `buildShowMenu` (app.js:1542-1581): list adds the Board · List row in `buildShowMenu` and swaps the caret. We should not touch them; but the peek must step aside while `.menu-up` is on (see §1d).
8. `enterApp` (~app.js:3120-3270) and `boot` (~app.js:3430): list edits the seed; 3599950 edits the replaceState line; import adds a hunk in init. If the plan needs a boot hook (e.g., reset the peek on crew switch), put it in `boot()` right after `closeShowMenu({ instant: true })` (app.js:3432-ish) — list's WIP edits 10 lines further down (pendingViewHint), so a separate line is safe.

**js/v3/wall.js** — PLAN U6 lifts `splitDays`/`knownDaysOf`/`groupByDay` (wall.js:720-760) and `weekendsOf`/`weekendRoom`/`wallPlanFor`/`roomsOf`/`roomsIn`/pure `dayNavOf` (wall.js:1355-1499+) into `week.js`. **Direct collision:** list's WIP inserts `listOffered` at wall.js:1442, i.e., INSIDE the block U6 wants to move, and import moves `searchFold` (wall.js:~874) right between the two blocks. A move-and-re-export in a file two siblings are editing is the worst merge shape. Options: (a) land U6's lift after list merges (then rebase-free merge of origin/live/list first); (b) do the lift as a COPY into week.js with wall.js keeping thin re-export stubs only after list lands; (c) do not lift at all for Our plan — import the functions from wall.js (they are already exported: `weekendsOf` 1355, `weekendRoom` 1364, `wallPlanFor` 1377, `roomsOf` 1422, `knownDaysOf` 733, `groupByDay` 748, `splitDays` 720; `roomsIn` is private). (c) is the no-conflict path.
   `nightMinutes` (wall.js:1724) is exported and is the "over" rule list uses; the plan's "Earlier today" should use the same function so the two past counts agree.

**assets/v3.css** — the dock block is v3.css:523-721 (`.dock` 523, `.days` 540-569, touch floor 625-653, `.now-tab` 683-691, `.menu-caret` 709-713, `.dock.hidden` 720, desktop hide 721); popover positioning 343; `.dock.menu-up, .day-rail.menu-up { z-index: 39 }` at 348; welcome card at ~186-225. List will rewrite `.menu-caret` (→ ☰ icon) and the fest-link's order (dot first). **Add the peek's rules as a NEW block at the end of the file** — but import (v96) also appends 81 lines at the end: when v96 lands on main and we merge it, two appends at EOF conflict trivially (resolve by keeping both). Better: put our block in its own marked section right after `.dock.hidden`/desktop rule (v3.css:721), which neither list's caret change (709-713) nor import's EOF append touches.

**index.html** — the dock is index.html:242-252 (`#dock-you` 243, `#dock-now` 244, `#dock-days` 245, `#dock-fest-wrap` 246-251 with `.menu-caret` 249 and `.sync-dot` 250); the rail index.html:217-236 (`#rail-now` 224, `.menu-caret.down` 232, `.sync-dot` 233, `#sync-label` 234). List will reorder inside the fest-links (dot before name, ☰ instead of caret) — lines 230-235 and 247-251. **Put the peek's element as a sibling BEFORE `<div class="dock" id="dock">` (i.e., after index.html:240 `</div>` / the comment at 241) or as the dock's first child** — the former keeps list's hunks disjoint. Removing `#dock-now`/`#rail-now` (the one-NOW rule's "tab leaves") must NOT be a markup delete: PLAN §2.7.3b keeps the tab as the fallback door, so the markup stays and only `paintNowTabs` decides.

**service-worker.js APP_CORE** (service-worker.js:22-62): new `js/v3/plan.js` (+ `week.js`, `plan-shelf.js` if made) must be listed or the offline shell is incomplete — `tests/app-shell-complete.test.mjs` recomputes `ASSET_STAMP` (service-worker.js:6). import inserts after `tools.js` (line 51) and after `name-rules.mjs` (41). **Insert ours after `/js/v3/motion.js` (line 61)** so the three hunks stay separate. Do not stamp (coordinator stamps).

### 1d. Behavioural collisions (not textual)

1. **Stacking:** `openShowMenu` raises the dock to z39 via `.menu-up` (app.js:1484-1492, v3.css:348) so its upward popover clears the welcome card (z38). A peek sitting on the dock's top edge is exactly where the dock's popover opens (`.dock .sort-pop { bottom: calc(100% + 8px) }`, v3.css:343). If the peek is inside `#dock` it rises with it (good); if it is a sibling it will cover the popover unless it hides/lowers while `.menu-up`. PLAN §2.5.4 (a top-level `--z-popover` layer) has NOT been built — v93 fixed the symptom with `.menu-up` instead. Update PLAN's §2.5.4 accordingly.
2. **List's past fold vs the plan's "Earlier today":** list removes over cards from the DOM under a frozen `ctx.pastAt`. If the plan ever reads the DOM (e.g., `nowLanding` reads cards: wall.js:1802), a list-folded card is gone. The plan model must read the data (fest + picks), never the wall's DOM; and the one-NOW gate calls `nowLanding` which DOES read the DOM — a live card is never "over", so fine, but a set that ended seconds ago might still be in the plan's NOW (plan uses its live window; list uses nightMinutes vs frozen clock). Use one rule (PLAN §2.6.2 `liveWindowOf`).
3. **Tap change (live/tap):** a phone tap opening the notes shelf means another bottom shelf. The peek must go away (or sit under) while any shelf is up — PLAN §2.3's companion/question roles. tap's brief keeps edits to card-facts.js and notes.js, so no textual collision, but both sessions will add "hide me while a sheet is up" logic — agree on one flag (e.g., `document.body.dataset.shelf` or the existing `closeSheet`/`leaveShelf` path in app.js).
4. **The 3599950 wall URL** changes the path to `/f/<fest>` — any rig or test of ours that asserts `location.pathname === '/'` breaks after v96. None exists yet; note for our tests.

## 2. The List view design vs Our plan

Sources: `claude-plans/2026-09-25-portola-live/design/list-view/BRIEF.md` (round brief), `list-view/d-full-width/BRIEF.md` (the approved list + dock), `list-view/m-menu-past-persist/BRIEF.md` (persistence and "over"), `list-view/c-the-moment/BRIEF.md` (the punted hour lens), and `claude-plans/2026-09-26-unified-build/LIST-BRIEF.md` (the build brief).

### 2a. Does any list design mention Our plan or the peek?
1. **No.** None of the list-view briefs mentions Our plan, the plan peek, or what the dock's top edge carries. d draws the dock only as `dot · PORTOLA '26 · ≡` (d BRIEF:119-136). So the list design leaves the peek undesigned, and nothing in it forbids it.
2. **Name collision, flag it:** m's fold calls the faded last-over row "the peek" (m BRIEF:184, 190, 198; m proto.css:25-77 `--peek`; m proto.mjs:109-266). Our plan's "peek" is a different thing. If list ships code with a `--peek` custom property or `.peek` class, ours must use a distinct name (suggest `plan-peek` / `--plan-peek-h`). d's approved riff dropped m's peek for the List (d BRIEF:96-100: "Everything over is gone"), and LIST-BUILD.md says over cards are removed — so the term may not reach code, but check before naming.
3. c ("the moment", punted per d BRIEF:30-31 and arc NOW "a design session for the hour pin and the moment lens") shares Our plan's vocabulary: "6 of us", "most of us" in `--brand`, faces per branch, THEN rows (c BRIEF:24-45, 62). When the moment lens is designed it should reuse plan.js's `usOf/barFor` and the same "n of us" wording, not a second count.

### 2b. The dock after List (what our peek sits on)
1. Order becomes **sync dot · FEST '26 · ≡** in the dock and the rail (d BRIEF:119-130; LIST-BRIEF item 3). The ≡ is a 13px SVG (12-unit viewBox, 1.4 stroke, round caps), body colour at .72, **`--brand` while the menu is open**, never `--fest`; no hit target of its own (inside the fest button).
2. **Cost:** the fest button grows ~14px, so the day row loses ~14px: at 390 SUN sits behind the row's fade; at 320 only SAT NOW show (d BRIEF:131-136). Kevin kept the year (LIST-BRIEF item 3). **Implication for us:** the one-NOW rule, once it removes the NOW tab while the peek shows NOW, GIVES this width back to the day row — a real argument for the rule, and it should be measured in our frames (390: does SUN come back whole when NOW leaves?).
3. The menu: room checks · divider · Board · List glyph row · divider · Settings (d BRIEF:128-129; LIST-BRIEF item 4). No Earlier row. Still v93's popover (no history entry). Nothing for Our plan in it — our entry point is the peek itself (and later the people menu's "Our plan" row, §3).
4. Desktop: the rail has the same order; its menu opens downward (d BRIEF:130). Our desktop corner card (bottom-right) and the rail's downward menu do not overlap.

### 2c. The past: list's copy vs the plan's copy
| | List (approved) | Our plan (round three) |
|---|---|---|
| Per-room fold | `EARLIER · 7 SETS ⌄` → `HIDE EARLIER ⌃` (d BRIEF:96-102); nouns "sets" (Portola, Afters) / "parties" (Folsom) (d BRIEF:115-117) | — |
| Whole days | `EARLIER · THU · FRI` at the top of the wall (d BRIEF:111-113); m had `· 48 ⌄` count (m BRIEF:197) | — |
| Plan's past | — | `Earlier today: 8 stops` (ours-r2 BRIEF:80; r3-proto.mjs:147-150), sentence case, bold count |
| Style | section micro-label: 10px/800, `--track-label`, uppercase, `--text-secondary`, bare `<button aria-expanded>` 44px tall, hairline after (m BRIEF:205-209) | plan row style (second-line Inter 12px/600) |
| Stored? | never (a reveal is for the page) (d BRIEF:114) | not specified |

**Recommendation:** the plan's fold should speak the app's one grammar once list ships: `EARLIER · 8 STOPS ⌄` / `HIDE EARLIER ⌃`, same micro-label, same button, same motion (m BRIEF:240-260). Two past doors with two grammars on one screen (the wall's `EARLIER · 7 SETS` behind the open plan's `Earlier today: 8 stops`) is the kind of inconsistency Kevin flags. The word "stops" stays (the plan counts stops, not sets). This is a design decision for Kevin's review page, not ours to settle silently — default to the list grammar.

### 2d. "Over" — one rule
1. List's rule (m BRIEF:137-158; LIST-BRIEF item 2): a card is over **exactly when its NOW ring can never light again**: per host, `clock = festivalClock(now, tz)`, `at = nightMinutes(iso, clock)` (wall.js:1724), over when `days ≥ 2` or `at ≥ data-now-to`; no-window cards are over when their NIGHT is; a night is over when `days ≥ 1` and every windowed card on it is over (the 5 AM rollover: Saturday Folsom's Aftershock 3-10 AM keeps Saturday alive at 6 AM Sunday).
2. The window used (m BRIEF:147-149): grid +60 min without a printed end; stacks: next act's start, else room close, else +60; by-time list: printed end, else the stack rule. This is the SAME three-rule split PLAN §2.6.2 wants `liveWindowOf` to unify — list is building on the three rules as they are (reading `data-now-from/to` off the DOM).
3. ours-model's past: stops with `to <= minutes` on the night's minute axis (ours-model.mjs:307-317 `oursAt`: current = `from <= m < to`). Its grid window is "no printed end → next set on its stage, else +60" (ours-model.mjs:78-82), which is NOT the wall grid's rule (+60 always, PLAN §2.6.2) — so the model already disagrees with the wall's ring for an open-ended grid set. Its time axis is events.js/time.js `activityMinutes` (9 AM starts the festival day: js/time.js:29-36, events.js:124), while the wall's NOW clock rolls over at **5 AM** (now.js:9-13) and reads "now" per night with `nightMinutes(iso, clock)` (wall.js:1724-1728: day 0 or day +1 only, +24h on the next day). **Porting rule:** set times on the 9 AM axis are fine (same as the wall's cards); the *minute passed to planAt/peekOf* must be `nightMinutes(nightIso, festivalClock(date, tz))` exactly as `positionNowMarks` does (wall.js:1729-1735), never a raw hour-of-day — else at 6 AM Sunday the plan and the wall disagree about Saturday's Aftershock.
4. **List freezes the fold** (`ctx.pastAt`, recomputed at boot/resume/day turn, never on the minute tick — m BRIEF:234-238; LIST-BUILD.md "The build" 3). **The plan's "Earlier today" must follow the same freeze** or, on the same screen, a stop leaves the open plan at 10:15 while its cards stay on the wall until the next jump. Simplest: plan-shelf reads the same `ctx.pastAt` list sets, when present.

### 2e. NOW in the list
1. LIST-BRIEF item 7: the NOW ring and the NOW jump must work in the List; "consider the production now line inside a time list"; rings may be heavy in a dense band.
2. m BRIEF:219-232: NOW lands as today; folded cards are never NOW stops; over days' tabs stay in the day row; the scrollspy treats the closed days line as part of the first open day.
3. c drew a band label that reads `NOW 1:50 PM` in brand (c BRIEF:62) — a third NOW surface if it ever ships. With Our plan's one-NOW rule, the list's band label must not become a NOW door (it is a marker, not a door). Say so in MODEL-V4 §3d's rewrite (§5 below).
4. **Consequence for our one-NOW gate:** `paintNowTabs` calls `nowLanding(wall-root)` (app.js:857), which reads the DOM. In the List, NOW lands on list cards, so the gate keeps working as long as list stamps `data-now-from/to` on its rows (m BRIEF:139-141 says time-list cards already carry them).

## 3. The people menu (Highlight) and how a highlight reaches the peek

Sources: `claude-plans/2026-09-25-portola-live/design/people-shelf/BRIEF.md` (design done 2026-09-25 late, on v92; nothing built), its `proto.mjs`/`proto.css`; PLAN.md top "Kevin's calls after the plan" item 2 (PLAN.md:8); LEDGER.md:73 (call #8); ours-r2 BRIEF round three and R3.

### 3a. The "Our plan" row in the Highlight menu
1. **Where:** the left twin of the Show menu, opened by the dock's avatar (today `#dock-you`, index.html:243, which jumps to top). Rows: `HIGHLIGHT` label · **Everyone** (✓ when nobody is highlighted) · one row per crew member (colour ring, ✓ filled when highlighted) · divider · **Our plan ›** · **Pick as someone else ›** · **+ Add someone** (people-shelf BRIEF:39-47). A guest's menu: Highlight rows, then **Our plan** and **Join the crew ›**; no "you", no Add (BRIEF:101-109).
2. **What it is:** "Our plan ›" in `--tonal-text` — "the other way into where we'll be" (BRIEF:45; proto.css:21-22 `.hl-pop .act.plan { color: var(--tonal-text) }`; proto.mjs:76 is just a row with a chevron, no handler). So it is a **second door that opens the plan** — on a phone, the peek grown open (the day plan); on a laptop (the Highlight menu drops from the rail's avatar, BRIEF:111-117), presumably the corner card grown into the 400px panel. The design draws no destination; Our plan must expose one `openPlan()` entry (not a drag-only open) so this row can call it later.
3. **Why it matters now:** it is the only door into the plan that works when the peek is NOT showing — e.g., a day with no plan today (fewer than 3 pickers → no peek, PLAN §2.6 "When it does not appear"), or on a tomorrow-planning morning. Decide: with no plan, does the row hide, grey out, or open an empty-state plan? Undesigned.
4. **Stale word:** the people-shelf design and PLAN item 2 say "+ Add someone"; v93 renamed it **"+ Invite someone"** (commit dc67f5c; app.js:797-807 `add.textContent = '+ Invite someone'`). The people menu build should use v93's word.
5. **Ordering:** PLAN item 2 slots the people menu "after U4 (the shelf primitive)"; arc NOW puts it before Our plan in the post-Portola order ("the people menu + Invite sheet, Our plan"). Our plan being built now in parallel means **the people menu does not exist when Our plan lands** — the "Our plan" row is future work for whoever builds the people menu. Our plan should not wait on it.

### 3b. How a highlight should show in the peek ("Ross's now")
1. **The approved design says:** "With a friend highlighted, the peek shows their now and a click lands the wall on their card (the NOW jump, moved)" (ours-r2 BRIEF:112-114 round three; same in R3, BRIEF:435-436). It is the reason NOW can leave the dock at all: Kevin's own NOW story is "tapping ross on the top to highlight him and then clicking something … to time bar" (app.js:835-838 comment; ours-r2 BRIEF:339).
2. **The plan says (not yet answered):** PLAN §5 Q4c default — "U7 ships without a highlighted peek, and a highlight keeps today's Ross story on the NOW tab until that row is drawn" (PLAN.md:815-822). Q4a default: a live set that is not a plan stop keeps the NOW tab beside the peek's NEXT. Q4b: a tap on the live card inside the open plan glides the wall to it. No `r4-*`/Q4 answers exist (OUR-PLAN-BUILD.md log: "there are no r4-* rows"); LEDGER call #5 says only "one NOW (in the peek) once it exists".
3. **What the code gives us:** a highlight is `ctx.filterPeople` (app.js:809-814 setPeopleFilter; it DIMS non-matching cards, wall.js:68). NOW already understands it: `liveOnWall` (wall.js:1784-1797) builds `picks` = live, non-dimmed cards the highlighted people picked, sorted by level; `nowLanding` (wall.js:1802-1809) lands on `picks[0]` with `match:true`, else the plain landing with `match:false`; `jumpToNow` then toasts `nothingOnFor(...)` (app.js:1072, 1211). So "their now" = `nowLanding(...).match === true` → the card. **The one-NOW gate must therefore consider the highlight:** while `ctx.filterPeople.length` and the peek is showing the CREW's NOW (not the highlighted person's), the NOW tab should stay (it answers "Ross's now", the peek answers "our now"). That is exactly Q4c's default, and it means the gate is `showTab = landing && !(peek.tag === 'NOW' && !(filterPeople.length && landing.match))` — roughly; get Kevin's call on the review page.
4. **If the highlighted peek is built (the design's intent):** the peek swaps its row to the highlighted person's live card (artist, place, `NOW till …`), the count becomes their face(s) rather than "n of us" (undrawn), and a tap runs `jumpToNow()` (app.js:1063) rather than opening the plan. Multi-select highlights (the people menu is multi-select, people-shelf BRIEF:54-57) make "their now" plural — `liveOnWall.picks` already sorts by level; the peek would show the top one and the NOW cycle (`nowStops`/`nowStep`) walks the rest. Undrawn; needs a frame.
5. The Highlight pill (people-shelf BRIEF:63-71: faces + ✕ in the avatar's slot, ~70px at 320) squeezes the day row further; together with list's ≡ (+14px) the dock at 320 is tight — another reason the NOW tab leaving (one-NOW) matters.

### 3c. PLAN "Kevin's calls after the plan" item 2, current status
Quoted at PLAN.md:8. Status on the current head: **nothing built.** The dock's left is still `#dock-you` (jump to top); the phone's people row is still in the toolbar (index.html:206 `#person-chips`, app.js:751-807 `renderPersonChips`); the "+ Invite someone" chip lives there (v93). The Show menu's component (`.sort-pop`, `showMenuRow`, `.menu-label` from v93 commit 43f80d1: "ready for the Highlight menu to come") is the reusable piece. When built, the Highlight menu inherits v93's popover rules (no history entry, stays open, busy flag `show-menu` — app.js:1482-1508), so `openMenu` becomes plural or generic; our peek must step aside for BOTH menus (the dock's `.menu-up` at v3.css:348 already covers any menu inside the dock).

## 4. First open, the guest shelf, and where the welcome card sits

Sources: `design/first-open/BRIEF.md` (F2 "wall first", shipped in v92), `design/guest-shelf/BRIEF.md` (the join shelf, shipped v92), ours-r2 BRIEF round three (the corner stack), PLAN §2.5/§2.6.6.

### 4a. The welcome card today (shipped code, current head)
1. Built by `welcome.js` as `div.bring-offer.welcome-offer` (welcome.js:155) — the bring-your-picks offer's anatomy (crew-entry.js:202-243). One CSS rule positions both: `.bring-offer { position: fixed; left: 12px; right: 12px; z-index: 38; bottom: calc(64px + env(safe-area-inset-bottom, 0px)) }` (v3.css:184-189); the inner `.bring-card` is `max-width: 440px`, centred (v3.css:190-195). It steps up by the toast's height + 8 when a toast arrives (welcome.js:132-145, a MutationObserver on `#toast-root`).
2. **Phone (390/320):** sits 64px (hand-typed "above the dock") over the bottom, full width minus 12px gutters, "covers the bottom third until Got it" (first-open BRIEF:214). z38: above the dock (30) and the zoom layer (36), below sheets (40-41) and toasts (50) (v3.css:180-183 comment; 482-484; 890).
3. **1280:** there is **no ≥720px rule for `.bring-offer`** (grep: v3.css only has 184) and the dock is `display:none` at ≥720 (v3.css:721). So on a laptop the card floats 64px above the window's bottom edge, **bottom-centre, 440px** — exactly first-open BRIEF §8.7 ("Desktop: the card sits bottom-centre at 440px max (the offer card's rule)", BRIEF:447-448). The 64px there clears nothing (no dock).
4. While the Show menu is open the dock/rail rise to z39 over it (`.menu-up`, v3.css:344-348; app.js:1484-1492).

### 4b. Collisions with the peek and the corner card
1. **Phone:** the peek sits on the dock's top edge (ours-r2 BRIEF:74-76). Dock height ≈ 9px + content + 9px + safe area (v3.css:524-527; content ≥ 44px by the button floor) ≈ 62-64px — which is why the offer's hand-typed 64px "clears the dock". With a ~56-60px peek row + grabber on top, **the welcome card (bottom: 64px) would sit ON the peek.** Nothing in any design draws the phone stack of welcome + peek (old map line 189 said so; still true — first-open, guest-shelf and ours-r2 are silent on phones). Options, to settle in our design pass: (a) the welcome card rises above the peek via `--foot-h` (PLAN §2.5.2d: `.bring-offer`'s 64px → `--foot-h`); (b) the peek waits for the welcome card to go (the offer already waits for it: welcome.js:19-20 "the offer waits for it to go"), then rises — which matches the "footer shelves are a family / take turns" rule (ours-r2 BRIEF:206-208; first-open BRIEF:216-217 "the card and the bring-picks offer must take turns"). (b) is simpler and keeps one companion at a time on a phone; (a) is what PLAN §2.5 builds toward. Also toasts (`.undo-toast` bottom 64px, v3.css:888-890) and the Spotify pill (70px, v3.css:167-169) sit on the peek unless moved to `--foot-h`.
2. **1280:** round three moves the welcome card from bottom-centre into a **bottom-right "corner stack"** with the plan card ("one right edge, 10px apart, newest on top, the wall still live"; ours-r2 BRIEF:115-121; frame `frames/DT-welcome-1280.png`, gitignored, on this Mac). That **changes v92's shipped desktop position** (bottom-centre) — U8 must either build the corner stack (welcome card moves right, 380px wide like the plan card?) or keep the welcome bottom-centre and accept two bottom companions in different places. PLAN §2.6.4 and U8 walk ("the welcome card stacked on the plan card") assume the stack. Needs a frame on our review page; no code today supports stacking (each companion positions itself with fixed CSS).
3. **Guest's dock:** the "you" slot is a dashed **+** ring (`.you-avatar.guest`, v3.css:718 on this head — the old map's 666-667 is stale; app.js:711-718 pulses it after "Got it"). The peek does not touch it; the people-menu design later makes the + open the Highlight menu (people-shelf BRIEF:101-109).

### 4c. "A guest sees the crew's plan"
1. The rule is PLAN §2.6.6 ("A guest sees the crew's plan (the crew link is the consent boundary)") and ours-r2 BRIEF:81-83 ("with v92 a friend can open the crew link and look around as a guest, so the plan itself is what gets shared" — the reason "Tell a friend" was dropped). Neither first-open nor guest-shelf mentions Our plan.
2. In code, a guest is a session with `guestOf === crew token` (app.js:89-90: `state.setWritePolicy(() => !(guestOf && ...))`), no `ctx.meName`. The plan model's "us" = `state.activePeople()` with a live pick (PLAN §2.6.3d; ours-model.mjs:61-68 `usOf`) — a guest is not a member, so the guest simply sees "n of us" where "us" is the crew, not including them. **Wording question:** "8 of us" read by a guest who is not one of us. first-open's copy already calls the wall "our crew's plan" / "This is The Portola Crew's plan for Portola" (first-open BRIEF:114, 274; shipped as welcome.js:71 "This is the crew’s plan for {fest}.", plus :77 "started this plan", :78 "Your plan for X is ready."). Consistent enough; flag for Kevin only if the frames read oddly.
3. **Word overload:** the welcome card says the WALL is "the crew's plan"; the feature is "Our plan" (head `SAT OUR PLAN`, ours-r2 BRIEF:204-205). On a first open with both visible (welcome card + peek), "plan" means two things. Low risk, but worth a look in the frames.
4. The guest's plan is read-only by nature (the plan writes nothing). A row tap that grows a card into the zoom inherits v92's guest rules (a guest's zoom − / + / composer open the join shelf; app.js:469-475, 561-600). Once live/tap lands, a guest's tap opens the notes shelf instead — the plan's "row tap grows its card" (PLAN §2.6 UI 3) should call the same card-open path so it follows whatever tap does.

## 5. MODEL-V4 §3d / §3.1 today, and what "one NOW, in the peek" must rewrite

### 5a. Current text (live/plan head; MODEL-V4.md unchanged by list/tap)
1. **§3d is STALE against v93.** It still says NOW is "A `NOW` button **before the day tabs**, in the dock and in the rail … outside the tab rows" (MODEL-V4.md:345-347) and its "Room" bullet still describes the retired squeeze: "NOW keeps only its dot, in a faint ring; where even that leaves less than one day, the fest name gives way with an ellipsis" (MODEL-V4.md:407-411). v93's D1 (commit cde8607, "NOW joins the day row … the dot and the squeeze retire") changed the code (app.js:839-854 comment; `showNowTab` app.js:907-954; index.html:219-224 comment) but v93's docs pass touched only §3a.4 and §3.1 (`git diff origin/main HEAD -- MODEL-V4.md`: two hunks, at 181 and 226). **So the spec of record has not described where NOW is since v93.** The rewrite must fix that first, then add the one-NOW rule.
2. §3d's other bullets (MODEL-V4.md:348-406) are still true on this head: "When" (only while something is live; the minute ticker brings/takes it — `tickClock` app.js:824-832 → `paintNowTabs` app.js:856); "What a tap lands on" (`nowLanding` wall.js:1802); "Tap again" (`nowStops`/`nowStep`/`stillThere`); "Said" (`#now-status`, index.html:298; `nowSaid`); the ring alone; "How it works gets no row" — **pinned by tests/docs-truth.test.mjs:210-212**: any How it works lesson whose words contain "now" fails CI. If Our plan wants a How it works row ("Our plan …"), its words must avoid the word "now".
3. **§3.1 (MODEL-V4.md:216-288)** is current after v93: the Show menu stays up, is a popover with no history entry, closes on tap-outside / Escape / the fest name, holds reloads via `body[data-busy]`, Settings wears the gear, `Show` is `.menu-label`. Nothing about the dock's layout or NOW lives in §3.1. List (Phase 1) will add the Board · List row and ≡ here — not ours. Our plan touches §3.1 only if the peek must step aside while the menu is up (say it in §3d or a new §3f, not §3.1).
4. §3e (by-time) mentions NOW only as part of "the same card" (MODEL-V4.md:449) — unaffected.

### 5b. What a dated rewrite for "one NOW, in the peek" must change (proposal: a new dated block at the top of §3d, "Since v93 / Since Our plan", keeping the history below, as §3.1 does)
1. **Where (v93 truth):** NOW is a tab IN the day row, right after the live day (`SAT · NOW`), dock and rail alike; the row's resting rule keeps the pair in view (390 FRI SAT NOW SUN, 320 SAT NOW); the fest name never gives way; nothing live → parked hidden just before the row (index.html:224/244). Delete the dot/ellipsis "Room" text.
2. **One NOW (Our plan):** the tab shows only while something is live AND the peek is not showing a NOW row (PLAN §2.7.2; the gate goes in `paintNowTabs`, app.js:856-862, whose placement machinery stays). Name the fallbacks: no plan today (lineup fest, solo crew, fewer than 3 pickers — PLAN §2.6.6), a live set that is not a plan stop (Q4a default: tab beside the peek's NEXT), and a highlight whose person has something live (Q4c default: the tab keeps "Ross's now" until the highlighted peek is drawn). Same rule for the rail vs the laptop corner card (U8.2).
3. **What the peek's tap does vs the jump:** the round-three design makes a tap/drag on the peek OPEN the plan (ours-r2 BRIEF:74-89; PLAN §2.6 UI 2 "A tap opens it too"), and moves the jump to (a) a highlighted peek ("a click lands the wall on their card", BRIEF:112-114) and (b) the live card inside the open plan (Q4b default). The spec must say which door runs `jumpToNow` (app.js:1063) in each state, and **where "Tap again" (the multi-stop cycle, MODEL-V4.md:374-399) lives** once the tab has gone — this is not drawn anywhere. Candidates: repeated taps on the open plan's live row cycle `nowStops`; or the cycle is dropped when the plan carries NOW (the plan's row is one answer, "most of us"). Needs Kevin's call; say the default in the rewrite.
4. **Said:** "Focus stays on NOW" (MODEL-V4.md:400-403) becomes "focus stays on the door that was pressed" (the peek or the tab); `#now-status` unchanged.
5. **The List view (Phase 1):** a List band label may read the time the clock is in (c drew `NOW 1:50 PM`, c BRIEF:62), but it is a marker, never a door; the one door rule holds in both views. Cards inside a closed Earlier fold are never NOW stops (m BRIEF:224-226 — list's own rule, restate once).
6. **Motion:** the tab leaving when the peek takes NOW uses v93's existing leave (fade + the row's FLIP, app.js:939-954); the peek's NOW/NEXT swap is the peek's own motion (ours-r2 R3: "its row slides out left and the next slides in from the right, counts cross-fade", BRIEF:439-441). Both on the same minute tick → sequence them (tab leaves first, then the peek's tag), or they read as one thing jumping from dock to peek. Storyboard it (memory: "motion is designed, not patched").
7. **Width:** with the tab gone the dock's day row gets its width back — which pays for list's ≡ (~14px, d BRIEF:131-136) and the future Highlight pill (~70px at 320, people-shelf BRIEF:65-69). State it as a consequence.

### 5c. Other docs that name the NOW door or the dock
1. **docs/user-flows.md** has **no NOW flow at all**: F9 "Day navigation" (user-flows.md:229-239) lists "You chip (jump to top), day tabs (scrollspy-active), fest name" — no NOW tab, even after v93. NOW appears only as "a control" in F5.7's tap-outside rule (user-flows.md:146-148). Our plan's docs pass should add NOW to F9 (tab after the live day; leaves when the peek carries NOW) and a new flow for Our plan (peek → drag/tap → the day; laptop corner card → Open ⌃ → 400px panel → Close ⌄ / Escape).
2. F3.1 (user-flows.md:75-79) says the guest's welcome card is "above the dock" with buttons "Got it · How it works" — the shipped words are "Pick shows" / "Look around" + "More info" link (welcome.js:67-70). Stale, not ours; mention to the coordinator. When the peek lands, "above the dock" becomes "above the dock (and the plan peek)" or the peek waits (§4b.1).
3. `claude-plans/2026-09-24-now-jump-build.md` is the NOW build log (history; exempt from docs-truth).
4. **README.md** says nothing about NOW or the dock (grep). CLAUDE.md names no NOW door. So docs-truth (tests/docs-truth.test.mjs) pins nothing about the tab's presence — the tests that do are code tests: tests/day-row.test.mjs (12 NOW refs), tests/now-jump.test.mjs, tests/browser/now-jump.test.mjs (1130 lines, 15 refs), tests/browser/fold-intent.test.mjs, tests/browser/zoom-still-hand.test.mjs, tests/browser/guest-tap-route.test.mjs. Any change to when the tab shows must keep these green or move their door (PLAN U7 tests 3).

## 6. Late update (re-fetched ~05:05 PT) — the neighbours moved while this was written

1. **Our own head moved:** live/plan is now 6d80134 (merged main #52, docs only: CLAUDE.md +21 lines on two browser-test traps and "browser history is shared state … a menu or popover takes no entry of its own; layers Back must close ride `js/v3/router.js`"; RUNBOOK; add-a-festival). No code moved, so every app.js / wall.js / v3.css / index.html line cited above still holds. OUR-PLAN-BUILD.md records the agreed rule: the peek and the open plan take **no history entry**.
2. **live/v93 is stamped v96** (7abb52b, `festival-nav-v96`) and has merged live/import (362ed55) and main (be2af3a). So v96 = v93 + import + 3599950 (`wallUrl`). When it reaches main, our next `git merge origin/main` brings: import's APP_CORE lines, import's app.js hunks (~64, after seedShowOnce ~1925, openSettings ~2320, init ~3547), `js/fold.mjs` (searchFold moved out of wall.js), and CACHE_VERSION v96. Expect the list branch's `seedShowOnce → seedViewOnce` rename to conflict with import's hunk "after seedShowOnce" — the coordinator's merge, not ours, but our merges of list/main will replay it.
3. **live/list committed step A** (c5e436b): the view persistence exactly as §1b describes (crew.js viewFromHash, filters.js loadView/saveView/viewIsSet/LIST/BOARD, wall.js listOffered, app.js seedViewOnce; tests/first-open-view.test.mjs, tests/view-persist.test.mjs). **New uncommitted WIP (step B, the List renderer):** `assets/v3-tokens.css` +11 (`--list-w: 560px`, after `--clock-gap`, tokens ~117-130); `assets/v3.css` +61 inserted after the `.time-list[data-clock]` media block (**v3.css:1394 on our head**, i.e. mid-file — not near the dock block 523-721 nor EOF); `js/v3/events.js` +40 around `bandOf`/`TIME_BANDS` (events.js ~362-430: `hourBandOf`, `LADDERS`, and **grid sets carry their own `liveFrom/liveTo` window = start → printed end else +60** into the time list "so switching Board and List never moves whose ring is lit"); `js/v3/wall.js` +76 in `renderCard` (~57), `venueGroups`/`timeGroups` (~1661-1750), `renderComposed` (~2271-2339), `renderExtra` (~2351-2407), `renderWallInner` (~2443). None of these overlap Our plan's planned edits, but: **list is baking the grid's +60 rule into a third place** — PLAN §2.6.2's `liveWindowOf` (events.js beside `parseEventTime`, events.js:129) will have to replace list's `liveFrom/liveTo` too. Coordinate: one rule, one PR, after list merges.
4. **live/tap** now has TAP-BUILD.md (e700c10): phone tap → ONE bottom shelf (card facts, − meter + row, thread, composer), the long-press goes, `tapOpensZoom` → `fingerHand()`, **one `setLevel` under the tap cycle, the zoom's step and the waiting pick** (TAP-BUILD "Took" 2), WebKit real in CI, a guest's − / + / note door take over the shelf's history entry. **Two consequences for us:** (a) `setLevel` becomes the single place a pick lands — the natural hook to invalidate the plan model (REVIEW-1 #2: the cache in `repaintWall` goes stale after a normal pick because taps call `refreshArtistCards`, not `repaintWall`); ask tap to expose a pick-changed hook or call a no-op `onPicksChanged()` we can fill; (b) the shelf "arrives like the join shelf … the wall dims" — while it is up the peek must be hidden or under it (z41 sheet > peek), and when it leaves the peek returns. Also: "the welcome goes on the first tap" (TAP-BUILD step 4) — affects the welcome + peek turn-taking (§4b.1).
5. Nothing on origin touches index.html's dock or the dock CSS block yet; list's step C (the ≡ and dot order) is still to come and will edit index.html:230-235 / 247-251 and v3.css:709-713.

## 7. Summary for the orchestrator (the load-bearing facts)

1. v93 (NOW in the day row, the Show popover) is on our branch but NOT on main; v96 (= v93 + import + wallUrl) is stamped and gating. Our base is sound; code line numbers here are current as of 6d80134.
2. Collisions: app.js `tickClock` (824-832) is the one near-certain textual conflict with list (both append); `repaintWall` (1628-1651) and `seenBand` (959-971) are ours to change with one-line, marked hooks; `paintNowTabs` (856-862) is ours alone. wall.js week lift (U6) collides with list's `listOffered` (inserted inside 1355-1499) and import's `searchFold` move — import the already-exported functions instead of moving them now. Put new CSS after v3.css:721, new markup before `<div class="dock">` (index.html:242), new APP_CORE lines after `/js/v3/motion.js` (service-worker.js:61).
3. Stacking: the dock rises to z39 only while its menu is open (`.menu-up`); PLAN §2.5.4's top-level popover layer was never built. The welcome card and toasts sit at a hand-typed 64px — they will land ON the peek unless `--foot-h` exists or the peek waits its turn.
4. Specs: MODEL-V4 §3d still describes the pre-v93 NOW (before the days; the dot squeeze) — our rewrite must first catch it up to D1, then add one-NOW. user-flows F9 has no NOW at all. docs-truth fails any How it works lesson containing "now".
5. Undrawn / undecided: where the NOW multi-tap cycle lives after one-NOW; the highlighted peek ("Ross's now", Q4c default = keep the tab); the phone welcome + peek stack; the desktop corner stack (moves v92's bottom-centre welcome); the plan's "Earlier today: n stops" vs list's `EARLIER · N SETS ⌄` grammar; the people menu's "Our plan" row with no plan today.
