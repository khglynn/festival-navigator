# Map: tests, gallery, frame rigs and the release gate (Our plan build)

Status: DONE (all six sections + §7) — mapped against head e1eb206 on 2026-09-26 (head moved to 6d80134 mid-map: a docs-only merge of main b03f2ba — CLAUDE.md, RUNBOOK.md, docs/add-a-festival.md; no code or APP_CORE file changed, so every code citation below holds). Sections appended as finished.

## 0. Head state (read this first)

1. Head `e1eb206` on `live/plan`; merge-base with `origin/main` is `0fd8fb7` (main has not moved: `git ls-remote` at 11:0x UTC shows main `0fd8fb7`). Remote `live/list` is at `c5e436b` (ahead of the `49d3e04` this branch was cut from); `live/tap` `f84b671`.
2. **The SW stamp is already stale at head, on purpose.** Last stamp commit is `08713d1` (v95 `--keep`); since then the v93 work (`9548b48` etc.) changed `assets/v3.css`, `index.html`, `js/v3/app.js|aura.js|settings.js|tools.js|wall.js` without re-stamping. `service-worker.js:5-6` still says `festival-nav-v95` / `376ec98a`; the fresh hash is `05a9ccc1`. So `npm test` at head = **1 known red** (see §5).
3. **`origin/live/v93` (7abb52b) is NOT in this branch.** It carries the v96 stamp commit plus the Portola-app import (`js/v3/import.js` 657 lines, `js/v3/import-match.js`, `js/fold.mjs`, app.js +99, v3.css +81, wall.js +18, settings.js +8, SW +7, and tests `tests/import-*.test.mjs`, `tests/browser/import-flow.test.mjs`). When the coordinator ships v96 and main moves, merging main in will bring those — expect app.js / v3.css / service-worker.js (APP_CORE list) conflicts near the dock and the SW list.

## 1. Unit tests in jsdom: how the app boots, clocks, fixtures

1. Runner: `node --test tests/*.test.mjs` (`package.json` scripts.test). Each file is its own process, so module state never crosses files; inside a file it is sticky (one boot per file).
2. **Three rig shapes**, pick by what the test needs:
   a. `tests/helpers/shell-rig.mjs` `bootShell({url, storage, fetch, reportKey})` (:19-65) — the REAL `index.html` + `js/v3/app.js` in JSDOM. Sets globals `window/document/history/navigator/location/localStorage` (:25-37), rAF = `setTimeout(0)` (:38), a no-op `IntersectionObserver` (:45-47), `fetch` (whole network; default throws, :48), `matchMedia` always `matches:false` (:50), no-op `scrollTo` (:54), a canvas stub (:56-57). Corrals `setInterval` so `close()` clears them (:59-62). `settle(ms=20)` (:68). Everything else (layout, getBoundingClientRect, animate) is zeros/absent. Peek/plan code that reads `matchMedia` for desktop or Reduce Motion will always see `false` here unless the test overrides `dom.window.matchMedia`.
   b. `tests/helpers/zoom-rig.mjs` `makeRig()` (:19-126) — module-level: a bare `#wall-root`, state + model + wall + card-facts imported after globals, a 2-person crew, `makeCtx()` mirroring app.js handleTap, `mountCard`, real `PointerEvent` helpers, `recordAnimations(window)` (:139-157, cancel reports late via microtask), `stubRect(node, box)` (:161-165). rAF is SYNCHRONOUS here (:26).
   c. `tests/helpers/warm-rig.mjs` — lie-fi for shell tests: `heldNetwork()` (:30-63, requests hang until `release/releaseFirst`), `cachesHolding(files)` (:65-70), `within(ms, ok)` (:73-80), `crewDoc(people, selections)` (:21-24), `SCREENS` list (:82), `buttonMatching(root, re)` (:87-89).
   d. Direct module tests with a hand-built JSDOM and `state.activateCrew(token, doc, fid)` — e.g. `tests/now-jump.test.mjs:25-58` imports `wall.js` exports (`nowLanding, nowStops, nowStep, ...`) and pins clocks by passing instants (`pt('2026-09-26T22:30:00')`, :59-60). This is the natural shape for a pure `tests/plan-model.test.mjs`.
3. **The canonical boot storage** for a shell test landing on the wall (`tests/day-row.test.mjs:50-66`): `url https://fest.kevinhg.com/#g=<made-up token>`, localStorage `fn_crews_v3` (`[{token,name}]`), `fn_me_v3_<token>` (my name), `fn_crew_fest_v3_<token>` (fid), `fn_welcome_v1: '1'` (skip the welcome); fetch answers `/data/festivals/index.json`, `/data/festivals/<fid>.json`, `/api/crew?` (the doc), `/api/festival-add?` (`{festivals:[]}`), 503 otherwise. Then poll `$('screen-app').style.display !== 'none'` (:68). Minute ticks: set `document.visibilityState='visible'` and dispatch `visibilitychange` after moving the clock (:72-73).
4. **Clock pinning — three mechanisms**:
   a. Per-file movable clock (`tests/day-row.test.mjs:20-36`): a `ShiftedDate` installed on `globalThis.Date` BEFORE the shell rig is dynamically imported (`await import('./helpers/shell-rig.mjs')`, :38), with `setClock(iso)`. It "wins over" night-clock. Copy this for any peek/plan shell test. Named instants already in use: `SAT_1030 = 2026-09-27T05:30:00Z` (Portola Sat 10:30 PM PDT, Soulwax on the grid, afters open), `MON_5AM = 2026-09-28T12:00:00Z` (nothing live), `SUN_3PM = 2026-09-27T22:00:00Z` (:32-34).
   b. Scoped swap (`tests/shell-v4.test.mjs:641-651`): `class Pinned extends RealDate` for the length of one function (`atFestivalTime`).
   c. `node:test` `mock.timers` — only the errlog/update tests use it (`errlog-*`, `update-check`, `update-row`).
   d. Suite-wide: `tests/helpers/night-clock.mjs` (:15-32), loaded via `NODE_OPTIONS="--import ./tests/helpers/night-clock.mjs"` with `NIGHT_CLOCK=<ISO>`; offsets Date only (timers and performance.now stay real); a test's own pin still wins.
5. **"Tests that pass by daylight"** (memory, 2026-09-25): the shell rigs run at REAL time unless pinned; NOW-only paths render only while something is live; on 2026-09-24 11:42 PM PT 48 tests failed with `getComputedStyle is not defined`. Rules: in app code reach DOM globals through `window.` (the rigs set `globalThis.window` but NOT bare `getComputedStyle`/`matchMedia`/`innerWidth`); pin a clock in every new boot test; a red that touches files the diff did not change → check the clock first. For Our plan this bites twice: the peek renders only when there is a NOW/NEXT, so an unpinned shell test sees a different DOM at night; and a peek that measures (`getComputedStyle(el)`, `innerWidth`) must go through `window.`.
6. **Fixture conventions actually in use** (vs. the brief's "made-up names only"): tokens are always obviously fake, of the form `<purpose>testtoken_0123456789` (e.g. `tests/day-row.test.mjs:41` with the comment "a made-up crew, never a real link"; `tests/helpers/zoom-rig.mjs:47`). Member NAMES are not made up: the suite uses `Kevin` (71 uses), `Drew`, `Ross`, `Nhu`, `Kat`, `Pegah`, `Sam` — `tests/helpers/fest-shapes.mjs:74` `CREW = { Kevin, Drew, Kat, Nhu, Pegah, Ross, Sam }` — the first names of Kevin's real crew (the project memory mentions Nhu, Kat and Ross by name). First names are treated as fine (CLAUDE.md "The public pid and the name in the crew may ride along"); tokens are the credential and never appear. For a new test, either reuse `CREW`/`stressCrew(fest)` or pick new obviously-invented names; never a `#g=` of a real crew.
7. `tests/helpers/fest-shapes.mjs` `stressCrew(fest)` (:115-136) casts a seven-person crew onto the cards that stress a given festival BY SHAPE (never by artist name) and returns `{selections, affinity, noted, roles, shapes}`; `shapesOf(fest)` (:27-66) classifies names as clock cells / short / rightmost / stacked / listed. The browser contracts use it to run every law on every shipped festival. A plan model test can use it to get a crowded realistic crew for any fest without naming an artist.

## 2. What binds a NEW module: app-shell-complete, sw-stamp, docs-truth

1. `tests/app-shell-complete.test.mjs` has three tests:
   a. :42-49 — walks static imports, re-exports AND literal dynamic `import('./x.js')` from `/js/v3/app.js` (:18-25) and fails listing every reachable module missing from `APP_CORE` (`service-worker.js:22-61`; APP_EXTRAS does not count, :27-33). Message: "add to APP_CORE in service-worker.js (and bump CACHE_VERSION): /js/v3/plan.js".
   b. :51-59 — self-check of the walker (needs `/js/v3/filters.js` and `/js/v3/now.js` in the core).
   c. :65-69 — `readStamp(sw) === assetStamp(sw)`: sha1 (first 8 hex) over every APP_CORE path + its bytes, CRLF-normalised for text (`scripts/sw-stamp.mjs:24-44`). **Any byte change to ANY APP_CORE file** (index.html, v3.css, v3-tokens.css, every js/v3 module, data/festivals/index.json) turns this red, and so does adding/removing/reordering an APP_CORE entry (the path is hashed too).
2. So for a new `js/v3/plan.js` imported by app.js:
   a. Not added to APP_CORE → 1a goes newly red (1c is already red at head, §0.2) — and the shipped build would die offline on the first missing import.
   b. Added to APP_CORE without stamping → 1a green, 1c red (already red; the fresh hash just changes again).
   c. `tests/sw-stamp.test.mjs` (:25, :32) tests the hashing itself, not the repo's stamp — unaffected. `tests/sw-data-network-first.test.mjs:28,246,314-317,331-334` reads `CACHE_VERSION`/`ASSET_STAMP` but only requires `>= v41` and self-consistency — unaffected by not stamping.
   d. Locally: `npm test` = exactly one fail (`the service worker stamp matches the cached assets`) — the same one at head. In CI, the `checks` job's FIRST step `npm test` (`.github/workflows/ci.yml:17`) fails and the job stops, so the Tokyo and night passes (:21, :26-29) never run on a branch push; `browser` (:40-50) is unaffected (no browser test reads the stamp). A preview deploy still happens (Vercel does not wait for CI).
   e. `node scripts/sw-stamp.mjs --check` exits 1 (:64-68) — the same fact from the CLI. **Do not run the script without `--check`**: no flag bumps CACHE_VERSION (:69-73); unknown flags refuse (:55-60).
3. How builders handled it before release:
   a. v88–v92 era: each unreleased builder commit re-stamped with `--keep` ("chore: re-stamp v92 (unreleased, --keep) — …", e.g. `623a50b`, `a73df70`), so branches stayed green, and the release commit did the bump ("chore: stamp v92 …", `315ecab`).
   b. v93/v96 era (current, the parallel-builders era): builders leave the stamp stale on their branch and the coordinator stamps once at release on a clean tree above origin/main (`7abb52b` "chore: stamp v96" on `live/v93`; the project memory "SW stamp races parallel agents"; `claude-plans/2026-09-26-unified-build/PLAN.md:284`, `:850` "Serialize the stamp"; `OUR-PLAN-HANDOFF.md:79-80`; `LIST-BRIEF.md:62-63`). Consequence to report honestly at the gate: "npm test: N pass, 1 fail = the stamp (expected, coordinator stamps)", and a red `checks` job on the branch that the coordinator's stamp commit turns green. Any OTHER red is real.
   c. One way to prove the rest of the gate while unstamped without editing the repo: stamp in a throwaway detached worktree (the RUNBOOK's gate-worktree pattern), run the three passes there, discard. The build session's own tree stays unstamped.
4. `tests/docs-truth.test.mjs` — what can bind the build:
   a. :38-55 — every path in README's fenced structure block must exist. Nothing forces a new module into README (the block lists `js/v3/app.js … router.js`, README.md:57-67), but adding a `js/v3/plan.js` line before the file exists goes red.
   b. :140-146 — `NOW.md` (repo root) ≤ 12 KB. :148-164 — every backticked repo path in root `NOW.md` and `CLAUDE.md` must exist (regex :152: relative, a slash, an extension or a trailing slash). The arc's own `claude-plans/2026-09-26-unified-build/NOW.md` is NOT checked.
   c. :172-213 — Settings → How it works: nine rows word for word and in order, mirrored from `claude-plans/2026-09-16-wall-v4/MODEL-V4.md` §3a.4. If Our plan gets a How-it-works row, it must be added to BOTH MODEL-V4 and `js/v3/settings.js` in order, and the test's `rows` array extended; the test also forbids a lesson row for "now" (:211-212) — a row saying "NOW"/"now" would trip it.
   d. :57-70 — no doc names an `npm run` script that does not exist.

## 3. Browser tests (tests/browser/*)

1. Runner: `npm run test:browser` = `BROWSER_TEST_REQUIRED=1 node --test tests/browser/*.test.mjs` (`package.json`). 20 files, 6215 lines. `tests/helpers/browser.mjs:6-14` launches Playwright Chromium headless, falls back to `channel:'chrome'`; with `BROWSER_TEST_REQUIRED` a missing browser throws, without it tests skip with `NO_BROWSER` (:16). CI job `browser` installs chromium only (`.github/workflows/ci.yml:49`) — **WebKit cases run locally only** (each file does `try { webkit = await (await import('playwright')).webkit.launch(...) } catch {}` and skips, e.g. `tests/browser/now-jump.test.mjs:24-27`). Locally: playwright 1.58.2 wants chromium 1208 / webkit 2248 (`node_modules/playwright-core/browsers.json`); `~/Library/Caches/ms-playwright` has `chromium-1208` and `webkit-2248` installed. 11 files use WebKit: by-time, fold-intent, guest-tap-route, now-jump, shell-contract, show-menu-stacking, stack-row, touch-ghost, zoom-chrome, zoom-notes-chip, zoom-still-hand.
2. Serving: `tests/helpers/static-server.mjs` `serveStatic(ROOT)` (:14-31) — the repo root on `127.0.0.1:<ephemeral>`, `no-store`, `/` → `index.html`, extensionless → `.html`, **no /api** (404). Two kinds of page:
   a. the real app at `${origin}/#g=<made-up token>` — used by now-jump, meter, heads, shell, show-menu-stacking, stack-row, zoom-chrome/-chips/-door-row/-still-hand/-notes-chip, guest-tap-route, error-report, fold-intent; /api is supplied per context by `ctx.route` (see 3).
   b. `gallery.html` — by-time-contract, hover-contract, show-links, touch-ghost-contract, strip-follow, zoom-door-row (§4).
3. **The app-boot recipe** (`tests/browser/now-jump.test.mjs:42-113`, the richest; others are copies with less instrumentation):
   a. `browser.newContext({ viewport:{width,height}, hasTouch, serviceWorkers:'block' [, reducedMotion] })` (:43; meter-contract :108 passes `reducedMotion` at context level).
   b. `ctx.addInitScript` stubs `navigator.serviceWorker.register`, seeds localStorage `fn_crews_v3`, `fn_me_v3_<t>`, `fn_crew_fest_v3_<t>`, `fn_welcome_v1='1'`, optional `fn_fold_v1_<fid>` (the Show menu's folded rooms, :62), and instruments `Element.prototype.scrollTo`/`animate` for assertions (:63-88).
   c. Routes, catch-all FIRST (Playwright tries the last-registered match first): `**/api/**` → 503, `**/api/crew**` GET → the doc (a per-page `structuredClone`, :93-97 — a shared object leaked between tests once), `**/api/festival-add**` → `{"festivals":[]}` (:100-104); guest-tap-route also routes `**/fn-i/**` (error reports) (`guest-tap-route.test.mjs:63`).
   d. `page.on('pageerror', e => { throw e })`, `page.clock.setFixedTime(now)`, `goto`, wait for `#screen-app` visible and `> 20` cards, `document.fonts.ready`, sleep 600 (:105-113).
   e. `pull(page)` = `import('/js/sync.js').then(s => s.pollSync())` — a remote change on demand after mutating `doc` (:117).
   f. `door = width >= 720 ? 'rail' : 'dock'` (:113).
4. **Clock control**: `page.clock.setFixedTime(date)`; to tick, set a new fixed time then `document.dispatchEvent(new Event('visibilitychange'))` (`now-jump.test.mjs:1017-1018`, `:1036-1037`). Named instants: `SAT_1030 = 2026-09-26T22:30:00-07:00`, `SAT_9AM`, Sat 7 PM (:641), `2026-09-28T05:00-07:00` nothing live (:1036), `ACL_SAT = 2026-10-03T20:00-05:00` (:919), ACL 9 AM (:847); zoom/meter tests use `2026-09-23T19:00-07:00`. Trap (memory "browser-harness-traps" #2): the fake clock runs page timers LATE on big walls — hold until the state appears, never a fixed hold; poll from Node in real time, never from a page `setInterval`.
5. **Real-input helpers** (all file-local; there is no shared input helper module):
   a. Touch tap: `page.touchscreen.tap(x,y)` — works in Chromium AND WebKit (`tapOn`, `show-menu-stacking.test.mjs:44-47`; guest-tap-route throughout).
   b. Finger drag, Chromium only, via CDP: `ctx.newCDPSession(page)` + `Input.dispatchTouchEvent` touchStart / N×touchMove every 16 ms / touchEnd (`swipe(cdp,x,y,dx,dy,steps)`, `stack-row.test.mjs:204-213`, sleeps 900 for the fling). **End a drag with the finger still** (pause, a last touchMove at the same point, touchEnd) or the fling eats the next tap on Linux CI (`guest-tap-route.test.mjs:145-158`; memory trap #6).
   c. Hold (long-press), Chromium: CDP touchStart, wait for `#zoom-layer .zoom-slot.shown`, touchEnd (`holdZoom`, `stack-row.test.mjs:232-248`; `holdOpen` in `zoom-chips-contract.test.mjs:102` is the named pattern).
   d. **WebKit has no drag input in Playwright** — no CDP, and `page.touchscreen` only taps. guest-tap-route fakes the scroll-cancel with synthetic `PointerEvent('pointerdown')` + `pointercancel` (:159-165). A peek drag-up tested in WebKit would need synthetic PointerEvents/TouchEvents (`isTrusted=false`) — so the drag threshold/settle contract can only be REAL-input in Chromium; WebKit can check taps (peek tap → plan), layout and the one-NOW rule.
   e. Mouse: `page.mouse.move/down/up/wheel` and `locator.click()` (desktop cases; zoom-still-hand drives a still mouse under a glide, `zoom-still-hand.test.mjs:134-202`). Keyboard: `page.keyboard.press('Escape')`.
   f. Reduce Motion: `page.emulateMedia({ reducedMotion:'reduce' })` (`now-jump.test.mjs:620`, `:796`, `:1004`) or the context option. Wider glyphs (Linux stand-in): an init-script `letter-spacing` bump on `.dock .day-tab, .dock .now-tab .word, .dock .fest-name` (`now-jump.test.mjs:50-58`; meter-contract has the card version).
6. **Contracts that measure `#dock` or the bottom edge** (the ones a peek above the dock can break):
   a. `now-jump.test.mjs` — `view()` computes `dockTop` from `#dock` (:124-126) and every landing asserts the card/line is between the chrome and `dockTop`; `rowView` (:1054-1060) the same for the 320 afters slide. If the peek is NOT inside `#dock`, a landing can pass while the card sits under the peek.
   b. `zoom-chrome-contract.test.mjs:80-91` — `window.__chrome(el)` returns `floor = #dock top`; `parkCard(..., 'floor')` parks a card `dy` above it (:104-120); asserts the zoom clears it. This mirrors `js/v3/card-facts.js:833-838` `dockTop()` (the zoom's floor, used by `place()` :893-895 and the scroll-away close :1514-1515). **Both read only `#dock`.** A peek rendered inside `#dock` (dock grows) moves the floor for free; a sibling element above the dock needs `dockTop()`, both test helpers, and the stack-row check updated together.
   c. `stack-row.test.mjs:330-340` — the zoom "clear of the dock" (`z.bottom <= dockTop - 7.5`).
   c2. `tests/zoom-geometry.test.mjs:102-109` (jsdom) builds a fake `#dock` with a stubbed rect as the floor — the unit twin of (b); and `zoom-chrome-contract.test.mjs:226-239` asserts that ABOVE 720 there is no floor (`m.floor === null`) and the bottom moves nothing — a laptop corner card / 400px panel that the zoom must clear would flip that assertion (PLAN U8's `rightChromeLeft()` / `--panel-w`).
   d. `now-jump.test.mjs:884-896` — 320×640: `#dock-you`, `#dock-days`, `#dock-fest-link` in a row, no overlap, `#dock-now` inside the day row; `:926-975` the day-row rest per fest/width (with `wide` glyphs); `:976-1000` NOW and day tabs take a finger 12px above/below; `:1001-1047` the clock brings NOW into the row after SAT and takes it out (animation = opacity + `translateX(-6px)`, transforms only; Reduce Motion: no animation).
   e. `heads-contract.test.mjs:124-213, 240-291` — which `#dock-days .day-tab` is `.active` while scrolling, to the page bottom (`scrollY + innerHeight >= scrollHeight - 2`, :157). A taller dock changes where "bottom" is reached; a peek that covers the last room's cards is not checked.
   f. `show-menu-stacking.test.mjs:50-101` — `#dock .sort-pop` stacks above the welcome card, `#dock.menu-up` while the menu is up; a peek must sit under the menu (z-order) and step back with `menu-up`.
   g. `shell-contract.test.mjs:161-191, 282-309, 520-545` — the Show menu popover on `#dock-fest-link` (aria-expanded, focus back), the dock's day list.
   h. `guest-tap-route.test.mjs:173-190` — taps the dock's day tabs / NOW after a flick.
   i. `hover-contract.test.mjs:85-110` (gallery) keeps probes `< innerHeight - 40`.
7. **now-jump.test.mjs's doors** (1130 lines). The door is `#${door}-now` with `door = 'dock'` (<720) or `'rail'` (≥720) (:113, :164-168 `tapNow`). Cases: :188-285 (390 / 1280 / WebKit 390): NOW sits in the row right after the live day, a tap lands the line; Ross → SAT AFTERS; Nhu → grid + line together. :286-302 grid right end. :303 Kat third column. :320-351 highlighted person with nothing on (quiet line + polite status text "Nothing of Kat’s is on right now — here’s what is."). :352 card replaced mid-glide. :387 320×568 tall set. :476-523 NOW taps go stop by stop and wrap (390/430/1280/WebKit). :524-603 highlighted stops, hand scroll resets the cycle. :604-631 repeat tap pulses line + label. :632 folded room only (`fold:['Afters']`). :659 dropped pick doesn't pulse. :694 repaint keeps the cycle. :751-792 polite status region, focus stays on NOW. :793 Reduce Motion instant. :820-868 ACL 305, outside the live window no NOW, Sunday 5:30 AM afters still NOW. :884 the 320 dock row. :926-1000 row rest + finger reach. :1001 clock brings NOW in/out. :1078-1130 320 slide of SAT AFTERS for Ross (Chromium + WebKit, Reduce Motion). **If the one-NOW rule hides `#dock-now` while the peek shows a NOW row, every 390/320 case that taps `#dock-now` needs a new door (the peek row) or a fixture where the peek shows NEXT** — e.g. at `SAT_1030` with Ross/Nhu picks live, the peek would show NOW and `#dock-now` would be hidden, so roughly two dozen test instances change door (18 direct `#${door}-now` / `dock-now` references at :162-1124; the loops at :188, :476, :976, :1078 multiply them). The 1280 cases use `#rail-now` and are untouched unless the desktop corner card also takes NOW. `zoom-still-hand.test.mjs:131-202` clicks `#rail-now` at desktop (the glide under a still mouse) — also a door to move if the corner card owns NOW on a laptop.

## 4. gallery.html

1. What it is: 1131 lines, `noindex`, loads the PRODUCTION stylesheets (`assets/fonts/fonts.css`, `v3-tokens.css`, `v3.css`, :9-11) and imports production modules directly — it never loads `index.html` or `js/v3/app.js`. **Not in APP_CORE**, so editing it never touches the SW stamp. `ops/prod-smoke.mjs:243` boots it on the base host and requires `#zoom-gallery .card` and `#events-wall .card` to render. `--fest` is set on `<body>` (:71).
2. Structure (top to bottom), three independent `<script type="module">` graphs:
   a. `#zoom-gallery` (:74-111) — THE ZOOM, every state live. Script at :439-842: fest `gallery-zoom-fest` (:453-470), crew activated with made-up token, `ctx` shaped like app.js's (:570-588: `onTap`, `onStep`, `wireZoom`, `onPeek`, `now: null`), `stateCard(label, artist, opts)` (:688-697) appends a captioned real `renderCard` to a `.zoom-row`. Rows: ladder, cells, event, names, edges, meter, crowd, chips (with case buttons), crowded (:84-109). Toolbar: Low power, Slow motion ×4 (:78-79; `watchZoomLayerForSlowMo` :661).
   b. `#events-gallery` (:113-136) — THE EVENTS, the composed day: the real `renderWall` on `gallery-events-fest` (script :843-1098). A FIXED clock: `const NOW = new Date('2026-09-27T06:30:00Z')` (Sat 11:30 PM PDT, :983) passed as `ectx.now` (:986) — the gallery pins time by ctx, not by Date. Crew picks `ESEL` merged into the one page-wide crew doc (:962-969; names Kevin, Drew, Kat, Nhu, Pegah), day/section notes via `state.recordNote` (:973-976), `repaint()` = app.js repaintWall minus sync (:1003-1015), room checkboxes stand in for the Show menu (`toggleFold`, :1043-1061) because the menu lives in the shell. `<ul class="events-states">` (:122-133) is the written list of what each room demonstrates — the convention is to add an `<li>` when a state is added.
   c. `#back-pocket` (:138-151) — a static fixture of a retired panel (script :1099-).
   d. The older static "V3 GALLERY" sections (:153-306), first script :307-436 (palette/aura/meter pieces). Includes `.demo-dock` (:298-305): a HAND-WRITTEN dock (`SAT SUN`, fest link, sync dot) — stale since v93 (no NOW tab, no menu); nothing tests it.
3. **How a state is added**: (zoom) push `[label, artist, opts]` into a row's array or call `stateCard` into a new `<div class="zoom-row" id="zoom-row-…">` under a `g-label` numbered caption; add the artist to the gallery fest + crew selections. (events) add entries to `FESTIVALS[EFID]` / `ESEL` and an `<li>` to `.events-states`. Every section drives the real module with a ctx; nothing is mocked except the network and the shell.
4. **What "peek at NEXT / peek at NOW / plan open" states would need**:
   a. The peek and plan must be renderable WITHOUT app.js: a module (e.g. `js/v3/plan.js`) exporting a pure model (`planFor(doc, fest, {now, folded, me})`) and render functions (`renderPeek(host, plan, ctx)`, `renderPlan(host, plan, ctx)`) that take `now` and the folded rooms as inputs (the gallery pins time through ctx, :983-986, and holds `folded` itself, :979). If the peek is assembled inside app.js (like the Show menu and the dock), the gallery cannot show it — that is exactly why the Show menu only appears there as checkboxes.
   b. A new `<section id="plan-gallery">` with its own module script (the file's convention: one graph per section, own fest id + token, :445-446 and :850-851) — or, cheaper, a block inside `#events-gallery` reusing `gallery-events-fest`, `ESEL` and `NOW`, which already has Saturday's grid (now line), SAT AFTERS (now mark at 11:30 PM) and Folsom by-time, so a NOW peek (Folsom Street Fair / Fatboy Slim style crowd picks) and a NEXT peek (move the clock earlier) come from the same data. Three frames at fixed clocks: NEXT (e.g. Sat 7 PM, nothing the crew picked live yet), NOW (11:30 PM), plan open (the whole day's rows). Hidden rooms: the existing room checkboxes should also drive the peek (the "hidden rooms stay hidden" rule becomes visible in the gallery for free).
   c. A phone-width frame inside a desktop gallery: the peek is `position: fixed` above the dock in the app; in the gallery it needs a containing box (e.g. a 390px-wide `transform: translateZ(0)` wrapper, which makes fixed children position against it) or a `.demo-dock`-style static render. The zoom gallery's `#zoom-top-probe`/`#zoom-bottom-probe` (:72, :437) exist for zoom edge cases — a peek fixed to the viewport bottom would sit over `#zoom-bottom-probe` and the lower zoom rows while scrolling the gallery, so keep the gallery's peek contained.
   d. If a browser contract for the drag/settle wants a stable surface without booting the app, the gallery section is that surface (the hover/touch-ghost contracts are the precedent); if it needs the dock, NOW door and one-NOW rule, it must boot the real app (now-jump's `openApp`).

## 5. The release gate (RUNBOOK) and one real run

1. Commands (`claude-plans/2026-09-25-portola-live/RUNBOOK.md:35-66` at head `6d80134`; CI mirror `.github/workflows/ci.yml:16-50`):
   a. `npm test` — the machine's zone (CI: UTC).
   b. `TZ=Asia/Tokyo npm test` (ci.yml:21) — a far zone; caught a Portola instant keyed on device date (2026-08-27).
   c. The night clock: `NIGHT_CLOCK=2026-09-27T04:30:00Z NODE_OPTIONS="--import ./tests/helpers/night-clock.mjs" npm test` (RUNBOOK:43-45, ci.yml:26-29) = Portola **Saturday 9:30 PM PDT**, headliners on the grid, afters live. `tests/helpers/night-clock.mjs:15-32` shifts `Date` by a fixed offset in every test process (NODE_OPTIONS reaches each file's process); time keeps moving; a test's own pin wins. For Our plan this pass is the one where the peek shows a NOW row at boot in every shell test — the pass most likely to find a bare DOM global or a missing null-check.
   d. `node scripts/validate-festivals.mjs` (ci.yml:30). Ran it now: `10 festival file(s): 0 error(s), 2 warning(s)` (ACL "Flight by Nothing" billed Sunday without a set; Tomorrowland Winter empty lineup) — rc 0.
   e. `npm run test:browser` "when the release is visual" (RUNBOOK:46-47) — Our plan is visual, so yes; WebKit only runs locally (CI installs chromium only), so a local run is the only WebKit evidence. RUNBOOK:48-55: check BOTH CI jobs by name — the ruleset requires only `checks`, so `browser` can be red under a green merge button; poll with `gh pr checks <n> --json name,bucket --jq '.[] | select(.name=="checks" or .name=="browser") | "\(.name) \(.bucket)"'` (`--watch` never returns while the staging deploy sits pending).
   f. CI also runs `npm audit --omit=dev --audit-level=high` (ci.yml:33).
   g. Then (RUNBOOK:56-70) an independent Sol review (`gpt-6-sol`, high since 2026-09-26) on the exact head in a detached worktree with `node_modules` symlinked, looping to 4 rounds, and a Sonnet real-browser walk at phone viewport with real input — the walker is NOT the builder (independent walks caught two bugs the builder's own walk passed, 2026-09-26); a third round finding the same kind of hole → cut the mechanism, not the feature. For this build the coordinator does stamp / PR / merge / prod smoke (`OUR-PLAN-HANDOFF.md:76-80`).
2. **One run of `npm test` at the default clock** (this Mac, Node v26.9.0, 2026-09-26 11:02 UTC, head `e1eb206`, `jsdom@30.0.1` matching the lock): **1045 tests, 1043 pass, 1 fail, 1 skipped, 26.9 s** (`duration_ms 26876`; wall 27 s).
   a. The fail: `the service worker stamp matches the cached assets (run node scripts/sw-stamp.mjs after changing any)` — `tests/app-shell-complete.test.mjs:68`, actual `376ec98a` (stored) vs expected `05a9ccc1` (fresh). Expected: the stamp is left for the coordinator (§0.2, §2.3b). Not fixed, per the brief.
   b. The skip: `CONCURRENT MERGES on real Postgres` — needs `DATABASE_URL` (a scratch Neon branch); skipped everywhere including CI.
   c. I did not run the Tokyo or night passes, nor test:browser (the brief asked for one run).
3. Gate-worktree pattern to report a clean gate while unstamped: `git worktree add --detach <scratch>/gate <sha>`, symlink `node_modules`, `node scripts/sw-stamp.mjs --keep` THERE, run the three passes + browser there, remove the worktree. The build's own tree and branch stay unstamped; the gate result then says "clean with a --keep stamp in a throwaway tree".

## 6. Frame rigs — which to reuse

Candidates (all under `claude-plans/2026-09-25-portola-live/design/` — there is no top-level `design/` folder; the brief's `design/...` paths are relative to that folder):

| Rig | Last commit | What it serves | Clock | Crew | Notes |
|---|---|---|---|---|---|
| `ours-r2/rig.mjs` (56 lines) | `d55d087` 09-25 22:56 | THIS worktree (ROOT = `HERE/../../../..`, :12-13) via `tests/helpers/static-server.mjs` | `page.clock.setFixedTime(now)` (:48) | `ours-r2/crew.mjs`: made-up Ana…Lu, sizes 6/9/12 (`crewDoc('portola-2026', n)`), invented picks on the real Portola lineup | The approved Our plan R3 frames came from it (`frames3.mjs` + `r3-proto.mjs` + `ours-model.mjs`) |
| `people-shelf/rig.mjs` (60) | `eaf2739` 09-26 00:23 | this worktree, same static server | fixed | its own `crew.mjs` (made up), `guest`, `highlight` options | ours-r2 plus the welcome keys set (:42-43) and a seeded highlight |
| `folsom-by-time/rig.mjs` (294) | `eaf2739` 09-26 00:23 | `APP=<worktree>` env, own http server, `/f/*` → index.html, a fixture JSON swapped in for `portola-2026.json`, writes recorded | init-script `Date` subclass from T0 that keeps moving (:100-104) — not page.clock | made-up Kevin, Maya, Jonah, Priya, Theo, Rosa | slice-stitching `roomShot` (:124-150): never `fullPage` (Chromium's full-page capture drops touch emulation → `(pointer: coarse)` stops matching, 44px heads draw at 24px) |
| `list-view/d-full-width/rig.mjs` (105) | `aba41c3` 09-26 03:52 (newest) | `APP` default = the `design-app` worktree (hard-coded absolute path, :22) with v93 files overlaid from git objects at `e3c20d5` (:23-26) and served-byte patches (`patch.mjs`) + `/__proto/` | fixed | people-shelf crew | tied to another worktree and a sha; list-view prototypes |
| `list-view/{a,b,c,m}` | `4227157` 09-26 03:12 | similar overlays | fixed | — | list-view only |
| `ours/rig.mjs`, `first-open/rig/rig.mjs` | `306f7d3` 09-25 18:34 | older | — | — | superseded by ours-r2 / people-shelf |
| `claude-plans/2026-09-01-walk-rig/*.cjs` | 09-01 | a REAL URL (preview or production) with a `TOKEN` env | real | a throwaway crew on the real DB | for detached real-input walks of deployments that survive a profile switch; **not** a no-database frame rig (previews share the production DB) |

**Best to reuse: `ours-r2/rig.mjs` (with `people-shelf/rig.mjs`'s storage keys), invoked from a new folder for this build.** Why: it serves the worktree it lives in (so it renders this branch's production code with no env var), it is the harness the approved R3 frames were made with (the builder can re-render R3 side by side with the built peek, same crew and clocks — `frames3.mjs` has `SAT_11AM`, `SAT_940`, `FRI_7PM`), and it already has every no-database guard: `serviceWorkers:'block'` + `navigator.serviceWorker.register` stub (:28, :38), every non-origin request aborted (:30), `/api/**` 503 with GET crew served from memory and writes refused (:31-35), `/fn-i/**` swallowed (:36), `fn_errlog_off_v1` (:43), `fold` (:44), `desktop:true` → 1280-style mouse layout (:26-28), `deviceScaleFactor:2`, `isMobile` + `hasTouch` on phones. Widths: pass `{width:390,height:844}`, `{width:320,height:568}`, `{width:1280,height:800, desktop:true}`.
Adjustments it needs:
1. Add `fn_welcome_v1='1'` and `fn_welcome_joined_v1='1'` (as `people-shelf/rig.mjs:42-43` and `list-view/d-full-width/rig.mjs:84-85` do) or the v92 welcome card sits over the bottom of the wall — exactly where the peek goes.
2. The fixed `page.clock` runs page timers late on a big wall (memory trap #2): fine for still frames; for motion or a drag sequence, the folsom rig's moving-`Date` init-script is the safer clock.
3. Capture viewport-sized shots (the peek is fixed to the bottom, so viewport shots are what you want anyway); if a tall stitched frame is needed, copy folsom's `roomShot` slicing — never `fullPage`.
4. Output: `frames3.mjs:126` writes PNGs into the design folder; `.gitignore` denies images by default, so frames stay local unless allowlisted. Put this build's rig under `claude-plans/2026-09-26-unified-build/our-plan/` and write frames beside it or to the scratchpad.
5. After main moves to v96 (`3599950`: the wall's address becomes `/f/<fest>#g=…`), `serveStatic` has no `/f/` → `index.html` mapping; a goto to `/#g=` still works (the app rewrites the address in place), but any reload under `/f/…` (the new-build reload, a test's `page.reload()`) 404s. The folsom and list-view rigs already map `/f/*`; if you need reloads, add that one line.
6. Crew: ours-r2's nine made-up members (Ana…Ivy, bar = max(3, ceil(9/4)) = 3) are the round-three baseline; `crewDoc(fid, 12)` exercises bar 3 with 12, and a `stressCrew`-style crowd is available from `tests/helpers/fest-shapes.mjs` if a 13+ crew (bar 4) is wanted.

## 7. What this means for the Our plan build (and what the plan's line numbers get wrong now)

1. **None of the plan's foundations exist on this branch**: no `--foot-h`, `footTop()`, `js/v3/plan.js`, `week.js`, `liveWindowOf`, `plan-shelf.js`, `tests/fixtures/plan-crew.mjs` (grep of js/, assets/, index.html, tests/ at head). PLAN U7 item 3 ("the peek joins `--foot-h`, so the zoom, `seenBand`, the shell and the toasts clear it with no further code — that is U4's payoff") assumes U4 shipped first. It has not. So either (a) the peek renders INSIDE `#dock` (the dock grows; `card-facts.js:833-838` `dockTop()`, now-jump's `view()`, zoom-chrome's `__chrome`, stack-row and zoom-geometry all track it for free), or (b) this build does U4's floor helper first and moves all five readers + `.shell`'s bottom padding together.
2. PLAN's test line citations are stale: now-jump is 1130 lines (PLAN says 982); the `#dock` measurements are `zoom-chrome-contract.test.mjs:80-91, 104-120, 172-180, 226-239` and `stack-row.test.mjs:317-340` (PLAN §U4 says 89-90/172-180 and 277-290).
3. New tests the plan names, and their homes: `tests/plan-model.test.mjs` (pure, module-level JSDOM like `tests/now-jump.test.mjs:25-58`, clocks passed as instants); `tests/plan-peek.test.mjs` (shell-rig boot with day-row's movable `ShiftedDate` installed before the dynamic import); `tests/browser/plan-shelf.test.mjs` (now-jump's `openApp` recipe; real drag in Chromium via CDP, taps only in WebKit — §3.5d). PLAN says "made-up names only" for `plan-crew.mjs`: `ours-r2/crew.mjs` (Ana…Lu) already is; the existing suite's `CREW` (Kevin, Drew, Kat, Nhu, Pegah, Ross, Sam) is NOT made up.
4. A new module must go in `APP_CORE` (else `app-shell-complete` 1a red) and must be reachable by a literal import from app.js for the walker to see it; the stamp stays stale for the coordinator — so the only acceptable local red is the stamp test. Keep the plan model a separate module that takes `now` and `folded` as arguments, so the gallery (which pins time via ctx, `gallery.html:983-986`) and the jsdom tests can render it without app.js.
5. **History:** CLAUDE.md gained a law at `6d80134` (2026-09-26): "a menu or popover takes no entry of its own… anything new that Back must close joins the router (`js/v3/router.js`) with those four cases as its tests, not a patch mid-release." Whether Back closes the open plan is therefore a design decision with a test cost, not a detail.
6. Gate honesty: report "npm test: N pass, 1 fail (the stamp — coordinator's), 1 skip (Postgres)"; run the Tokyo and night passes too (the night pass is where a peek bug surfaces at boot); WebKit evidence exists only from a local `npm run test:browser`.

