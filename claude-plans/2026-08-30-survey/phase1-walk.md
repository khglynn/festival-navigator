# Phase 1 walk — results (2026-08-30)

Walker: Sonnet teammate, real pointer input via mcp__plugin_playwright_playwright__*.
Preview: notes-desktop-round, SW build under test noted per item.
Crew: zz-walker throwaway crew (token + person id reported to lead only, never written here).

Legend: PASS / FAIL / SKIPPED (not deployed) / BLOCKED

**Scorecard (checklist items 1-20): 12 PASS, 8 FAIL.** FAILs: #6 (wheel doesn't close zoom), #8 (Tab-to-focus doesn't grow), #11 (touch lift falsely picks), #15 (Settings order swapped), #16 (case-collision rename bug — high severity), #17 (old copy still live in 2 places), #18 (date line not short + no Fri/Sat/Sun day tabs), #19 (notes-sheet sub-line missing the date). Items #A/C done partially, #B done, #D not done — see bottom section.

Reused an existing throwaway crew already present in this browser profile (`zz-walk-0830`, Portola, members zz-Ava/zz-Ben/zz-walker, 2 pre-existing picks) rather than creating a duplicate — see report for token/person-id. Left a separate real-looking "Ava, Ben, Dev +3" Portola crew and a solo "Ava" Lost Lands board untouched (not mine, possibly another agent's/older fixture). Lost Lands testing (18-19) used the existing solo "Ava" Lost Lands board read-only (no picks/members added — items 18-19 don't require any).

## Desktop (1440×900)

1. PASS — hover Groove Armada (Sat/Warehouse, non-edge). Grown card center exactly matches resting card center (deltaX=0, deltaY=0 px). Neighbours (left/right/below cards) rects byte-identical before vs during zoom. Grown card confirmed living in `#zoom-layer`, a direct `<body>` child (not inside `.wall-grid`).
2. PASS — 5 clicks on grown card body cycled level 1→2→3→4→0 exactly. You pill appeared at click 1 (stayed present through 4, gone at 0). `<b>MUST</b>` appended inside pill only at level 4. Wash gradient (`.z-surface` radial-gradient) visibly intensified each level, cross-fading back to flat color at 0. Zoom stayed up (1 child in `#zoom-layer`, class `zoom-slot shown`) through all 5 clicks — no flicker, no re-grow.
3. PASS — clicking `.f-chip.notes` opens the notes sheet with "Groove Armada" as header and "4:45 – 6:00 PM · Sat · Warehouse" sub-line (matches card exactly). Grown card removed from `#zoom-layer` the moment the sheet opens. Escape closes the sheet only — URL/board unchanged, wall still there underneath.
4. PASS — moving mouse off the grown card (onto header) reverted it within the check window: `.zoom-slot` gone, resting card `opacity:1`, class exactly `"card cell"` (no leftover `zoom-source`), zoom-layer empty.
5. PASS — hover→grow, then Escape closes it; verified it stayed closed for 800ms with the mouse still stationary over the same card (did NOT regrow on its own). Moving the mouse away and back onto the same card did regrow it.
6. **FAIL** — hover→grow, then one wheel notch (`page.mouse.wheel(0,150)`) over the grown card. Expected: closes. Actual: it stays open. Verified twice, confirming the wheel event was genuinely processed (wall's internal scroll container's `scrollTop` moved 373→523 in the same call) while `#zoom-layer .zoom-slot` remained present both immediately before and ~500ms after the wheel tick.
7. PASS — clean `PerformanceObserver` session (`longtask` + `layout-shift`, un-buffered, started fresh) across 10 hover-in/hover-out cycles on 10 different Saturday cards (Robyn, Tove Lo, Fcukers, Oskar Med K, Skepta, DJ Shadow, Nimino, Tricky, Max Styler, Sam Alfred; ~450ms hover + ~350ms hover-away each). Result: **0 long tasks >50ms, 0 CLS layout-shift entries.** (An earlier buffered-observer pass showed 2 old long tasks and 1 tiny `fest-link` shift — those were pre-existing/page-load noise from before the observer's real window, not attributable to the zoom; the clean re-run isolated to only this test's timeframe shows zero.)
8. **FAIL** — Tab focus onto a lineup card does NOT grow it. Verified twice (waited 400ms then again 700ms after landing focus-visible on "Dog Blood"): `#zoom-layer` stayed empty both times, `document.activeElement` was the card itself (class stayed plain `"card cell"`, no `zoom-source`). Enter still functionally picks the focused card even with no visual grow (Dog Blood "not picked"→"picked" on Enter). Follow-on oddity: continuing to Tab from the ungrown Dog Blood moved focus to the *next* card ("Robyn") rather than into Dog Blood's own notes chip, and by the following read *that* card had gained `zoom-source` + a live zoom slot — i.e. whatever triggers the grow-on-focus is lagging/off by one Tab stop within the windows tested, not simply "instant like hover." Did not chase further Tab/Shift+Tab chip-focus assertions once the base premise (grows on focus) had failed twice.
9. PASS — grew a card via hover, then typed one character into search. Zoom closed (`#zoom-layer` empty) immediately after the keystroke.
10. PASS (minor note) — hovered "Despacio" (rightmost stage column, right-edge of `.times-scroll`; at 1440px the scroller doesn't actually overflow — `scrollWidth === clientWidth`). Grown card clamped horizontally (`right: 1412px` inside the 1440px viewport). One outlier: Despacio's card is an unusually tall 668px (it's the all-day continuous sound-installation stage, not a normal timed set), and its grown bottom edge sat at `901px` vs a `900px` viewport — a 1px overflow, not a visually meaningful clip. Flagging for awareness, not counting as a fail.

## Phone (390×844, real touch via CDP `Input.dispatchTouchEvent`)

11. **FAIL (partial)** — press+hold "Nimino" ~650ms: grows correctly, neighbours don't move (see #14). But lifting the finger (`touchEnd` right after the hold, no separate tap) **registers a pick** — "Nimino — not picked" → "Nimino — picked" immediately on lift, both times reproduced independently (once standalone, once again ahead of the flick-scroll test in #13). Checklist expects the level to stay unchanged on lift. Reads like a missing `preventDefault`/synthetic-click suppression on the hold gesture — the browser's default touch→click compatibility behavior is very likely firing a click through the pick handler on release.
12. PASS — with the card already grown, 3 quick taps on its body advanced the level 3 times in a row (×2 → ×3 → must) with the grown card staying up throughout (never re-collapsed between taps).
13. PASS — tapping outside the grown card closed it. A flick gesture (touchstart + 10 touchmove steps scrolling up + touchend) while a card was grown also closed it.
14. PASS — same neighbour-rect comparison as #1, done for the phone hold-hover: left/below neighbour cards' rects were unchanged during the ~650ms hold.

## Settings

15. **FAIL** — actual Settings order is **Your festivals → Crew → You**. Checklist expects Your festivals → You → Crew (Crew and You are swapped).
16. **FAIL (high-severity, confirmed + fixed)** — renamed "zz-walker" to "zz-ava" (a case-only variant of the existing member "zz-Ava"). The app DID show the expected refusal toast ("Someone in the crew already has that name — pick one that differs by more than capitalization...") — but the rename went through anyway: both the crew-pill list and the "You" card immediately showed "zz-ava", and it was still "zz-ava" after a full hard navigation/reload (confirmed via a fresh `fetch('/api/crew?...')`-backed reload, not just client state) — i.e. the server actually persisted a case-only-colliding name, not just a client-side optimistic-update-not-rolled-back glitch. This directly violates the case-insensitive uniqueness invariant this app's own docs call out as enforced ("Active member names must be unique case-insensitively... enforced in the merge's WHERE clause"). Side effect: the crew's sync status flipped to "can't sync" and stayed there across a reload. Recovery: renamed again to a fresh unique name ("zz-walker-restore") — that one persisted correctly and sync returned to "online" (verified `/api/crew` now returns 200). So the rename mechanism itself works for non-colliding names; specifically the case-collision REJECTION path fails to roll back / fails to actually block the write server-side. **Crew's "You" display name is now "zz-walker-restore", not "zz-walker" — flagging for the lead in case anything downstream keys off the literal name "zz-walker".**
17. **FAIL** — neither copy location uses the new wording. Wall coach-mark (captured at walk start): *"Tap artists to add your color. 4 taps = MUST SEE. Hold for details. Tap a name or a stage to see just that."* How-it-works page: *"Tap an artist to add your color. Brighter each tap. Four taps = I MUST SEE THIS."* and *"Hold a card for notes."* — both still say "MUST SEE" (not "MUST") and "Hold for notes"/"Hold for details" (not "Hold for a closer look"), which the checklist explicitly calls out as the old copy to remove.

## Lost Lands (used the pre-existing solo "Ava" board — read-only, no data touched)

18. **PARTIAL / mixed** — Day-rule wording is correct: section headers read "WEDNESDAY" / "THURSDAY" (not shouted/all-caps-shouted as a pre-party label), with "Early Arrival Pre-Party" as the smaller sub-line underneath — matches spec. BUT the day-tab row only shows WED / THU — **no FRI/SAT/SUN tabs exist.** The main 3-day festival (Fri–Sun) has no per-day set-time data yet (rendered as one undifferentiated "THE LINEUP · BILLING ORDER" list), so there's nothing to tab between for those days — this is very likely a data-completion gap (no set times loaded for the main festival) rather than a UI bug, but it does deviate from the checklist's literal "WED/THU/FRI/SAT/SUN" expectation. Separately, the "date line" is **not short** on either screen checked: both the pick-fests screen and Settings show the full sentence `"September 18–20, 2026 (main festival, Fri–Sun); a separately-billed "Early Arrival" pre-party runs Wednesday Sept 16 and Thursday Sept 17"` verbatim — that's a genuine FAIL against "date line ... is short."
19. **FAIL (partial)** — opened the Wednesday notes sheet: title reads "WEDNESDAY" (correct, weekday not the label), but the sub-line shows only "Early Arrival Pre-Party" — **no date**. Checklist expects the sub-line to carry both the date and the aside; the date portion is missing here.

## Console
20. Third-party noise only, reproduces on every page load regardless of interaction: `Failed to load resource: 404` + a follow-on MIME-type-refusal, both for `/_vercel/insights/script.js` (Vercel Analytics script absent/blocked on this preview deployment) — not app code. One additional error appeared during the item-16 rename-collision bug (`400` on `/api/crew`) — that was the server's (correct) rejection of the colliding rename; it kept echoing in the tool's console-message list across subsequent reloads even after the underlying issue was fixed, which turned out to be a stale/cumulative reporting quirk in the console-messages tool, not a live error — confirmed by a direct `fetch('/api/crew?...')` from the page returning a clean `200` after the rename-restore. So: zero live app-code errors by the end of the walk.

## Data-hygiene note (corrected after inspecting localStorage — see report for full detail)
The "LOST LANDS '26 listed twice" in Settings is NOT a dedup bug — this device's person record (`fn_person_v1` in localStorage) shows it genuinely belongs to **two distinct Lost Lands circles**, both named "Ava": one pre-existing (`NZsjmZQalgQMcY2jtQzBCEqWnd8`) and one my "+Add a festival" action created fresh (`puBbvNnopARgzgYdV4QytLxldK4` — this is the one I actually read from for items 18-19). So "+Add a festival" does NOT reuse an existing (person, festival) board when you already have one — it silently creates a redundant new one every time. That's arguably a real product finding worth the lead's attention on its own, separate from anything specific to this branch. The extra board I created has no picks/members/notes — purely read-only browsing — so it's inert, but the lead may want it deleted for tidiness. This device's localStorage also shows a fourth, completely untouched-by-me crew (`oNBjnJLXVQedBJm3k7JAl5Hv9oY`, "zz-design") — confirms this Chrome profile carries fixture data from prior test sessions, not anyone's real festival plans.

## Kevin's four specific checks

(A) Edge-hover centering — not separately re-tested beyond #1 (mid-grid) and #10 (right edge); #10's Despacio-column hover showed correct horizontal clamping (`right: 1412` inside `1440` viewport) with no left-edge test performed. Recommend a dedicated top/bottom-of-viewport + far-left hover pass if this needs harder confirmation — ran short on turns to do it as its own isolated measurement.
(B) Click-during-opening-animation — done in one atomic in-page call (no round-trips): real mouse move onto an unpicked card ("Robyn"), waited exactly 80ms (measured 87ms actual), then a real click at the same point. Result: the pick registered correctly on Robyn ("not picked"→"picked"), **no fall-through to a neighbour**. Caveat: at 87ms the click landed on/near the card's original resting position, and since the grow is centre-anchored (per #1, zero positional delta), this doesn't cleanly distinguish "hit the still-morphing grown element" from "hit the resting element underneath" — both are colocated early in the animation, so this is a faithful but not fully isolating measurement. No fall-through and no missed click either way, which is the meaningful part of Kevin's question.
(C) Pick-while-zoomed visual trace — captured via DOM inspection rather than a recorded visual trace: click 1 → `.f-pill.you` appears (background `rgba(245,100,71,0.42)`, text "You"); clicks 2-3 → pill stays, `.z-surface` radial-gradient wash visibly strengthens each step (first color-stop goes from translucent to fully opaque rgb, matching "wash cross-fades"); click 4 → `<b>MUST</b>` appended inside the pill; click 5 → pill and wash both revert to the click-0 state. Did not confirm the "GROW IN not pop" / "neighbours slide" claims specifically — neighbour rects were confirmed *unchanged* in #1/#14 (nothing else on the wall moves), and no neighbour-sliding was observed or expected to be since only the target card's own pill/wash update per click.
(D) Frame-0 no-jump — not measured with `document.getAnimations()` timing or the DevTools Animations panel; #1's centre-delta-0 finding is a strong indirect signal (the grown card starts exactly where the resting card was, so there's no positional jump), but Kevin's ask for actual first-frame timing data was not done. Flagging as unfinished rather than guessing.

