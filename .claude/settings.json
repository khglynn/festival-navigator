{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "compact",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"POST-COMPACTION — festival-navigator. Re-ground from CLAUDE.md (the rules) and NOW.md (where things stand, one screen) before acting. Open DEVLOG.md or claude-plans/archive only when the task needs history, and search them rather than reading them whole. Post-compaction sessions drift toward doing the minimum — refuse it.\"}}'"
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"systemMessage\":\"Compaction imminent (festival-navigator) — update NOW.md in place before anything else: replace stale lines with the live state and the exact next step, keep it to one screen, never add a dated section. The story belongs in DEVLOG.md.\"}'"
          }
        ]
      }
    ]
  }
}
